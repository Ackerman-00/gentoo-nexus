# gentoo-nexus autonomous-run relay state

status: in_progress
run_id: 32375534412

## Run summary (run 32375534412, RELAY=true, 2026-08-20 13:39Z+)

### Both workflow blockers FIXED by owner (commit 096e656) and VERIFIED

The owner applied both fixes from issue #5 in commit `096e656` ("updated",
2026-08-20 13:39Z, HEAD):

1. **rust-bin `abi_x86_32`**: `dev-lang/rust abi_x86_32` +
   `dev-lang/rust-bin abi_x86_32` added to GAMING heredoc in build.yml (~L422-423)
   and build-gentoo-official.yml.
2. **gpm/ncurses cycle**: heal trigger broadened in both workflows (~L640) to
   fire when installed ncurses lacks `gpm` OR `abi_x86_32`, then bootstrap
   `ncurses -gpm -> gpm -> ncurses +gpm`.

**Both VERIFIED in docker** (container `repro`, stage3:amd64-desktop-openrc,
builder make.conf/package.use replicated): heal fired (installed ncurses had gpm
but lacked abi_x86_32); bootstrap succeeded; `emerge --pretend` of
`=x11-misc/colord-1.4.8-r1` (26 pkgs, 24 binaries) and `=app-office/obsidian-1.13.7`
(17 pkgs, 14 binaries) resolved with NO circular dep. `=gnome-base/librsvg-2.62.3`
pretends clean with the rust lines present.

### Actions taken this run

1. **Triggered Build Relay** (run 32378014205, 14:05Z) with the 8 stale overlay
   packages: obsidian → rootapp-bin → protonplus → vesktop → brave-origin-bin →
   zen-browser → niri → noctalia-v5, `force_rebuild=true`. Chain builds one per
   run and relays the rest.
2. **Triggered Build Gentoo Official** (run 32378017240, 14:05Z) with the 8
   packages that failed on the blockers: colord, librsvg, openal, libsdl3,
   networkmanager, pam, libcap, gdbm, `force_rebuild=true`.
3. **Pinned noctalia-v5 to a064c063f204** (upstream pushed a new commit after
   the chain was dispatched; pin was 02286bbdcfa8). Commit `2583105`
   (chore(auto-update)) incl. regenerated md5-cache, pushed as
   opencode-agent[bot]. The relay chain checks out main at each run's dispatch,
   so the noctalia run will build the new pin. niri + xwayland-satellite pins
   still match upstream HEAD.
4. **Manifest/SRC_URI verification COMPLETE**: all 6 stale packages' Manifests
   match real distfiles (obsidian sha512 1e12f22c…, vesktop 9d24fdb9…, brave
   e6e8beb2…, zen 45cb449a…, protonplus 77605a6e…, rootapp-bin cd43adcf…).
   `ebuild manifest` re-verification in container: all 15 versioned ebuilds OK.
5. **Version accuracy sweep COMPLETE for all 18 packages** (all current;
   corrected repos: faugus-launcher = Faugus/faugus-launcher 2.1.0 (tag pushed
   2026-08-13; v1.1-24 is an older 2024 tag that sorts first),
   mangowm = mangowm/mango 0.16.1, brightnessctl = Hummer12007 0.5.1,
   scenefx = wlrfx 0.5, xcur2png = eworm-de 0.7.1, icoextract = PyPI 0.3.0).
6. **Consumer NO-SOURCE-REBUILD sweep** (container `sweep`,
   stage3:amd64-desktop-openrc + machine/ config + rolling binrepos):
   9 of 15 versioned overlay packages install as BINARIES (brightnessctl,
   cliphist, nwg-look, icoextract, faugus-launcher, mangowm, matugen,
   scenefx, xcur2png). The other 6 (rootapp-bin 0.9.127, obsidian 1.13.7,
   protonplus 0.6.4, vesktop 1.6.7, brave 1.93.137, zen 1.21.15b) currently
   resolve as SOURCE — exactly the stale set being rebuilt in the triggered
   chain. No unexpected source rebuilds.
7. **Config sync audit**: machine/ vs workflows CPU_FLAGS_X86 + COMMON_FLAGS
   identical; USES heredoc byte-identical between build.yml and
   build-gentoo-official.yml (16 lines); GAMING heredoc (138 lines) vs
   quickstart steam block (144 lines): differences are only the known exclusions
   (systemd, extest, egl-gbm/egl-wayland/egl-wayland2/egl-x11, nvidia-drivers)
   plus pre-existing flag deltas (llvm `-xml`, libglvnd `X`, compiler-rt in
   GAMING only — harmless unused builds, NOT introduced by the owner's fix).
8. **Dep audit**: all 18 ebuilds' DEPEND/RDEPEND/BDEPEND sane; all pass bash -n.

### CI status (as of checkpoint, ~14:25Z)

- Build Relay 32378014205: in_progress (Build Package in Docker)
- Build Relay 32376099904 (leftover relay from the old failed obsidian chain,
  dispatched 13:45Z post-fix): in_progress, ~36m
- Build Gentoo Official 32378017240: in_progress (Build Package in Docker)
- Note: two Build Relay chains running concurrently both start with obsidian;
  watch for rolling-release upload conflicts.

### Issues / PRs

- #5 (open): both blockers now fixed in main + verified in docker + builds
  triggered. Reply + close once a Build Relay AND Build Gentoo Official run go
  green (with evidence). Not closed yet.
- No open PRs.

### Remaining work

- Wait for both chains to progress; verify obsidian + colord builds go green
  (proves both fixes in CI).
- Re-run consumer sweep after builds: all 15 overlay packages must resolve as
  binaries.
- Reply + close issue #5 with CI evidence once green.
- Full-binary install sweep (STEP E) + final repo sweep.
- Finish with relay `status: complete` + `run_id: 32375534412`.

### What was NOT yet done (honest gaps)

- Install-test of the newly built binaries (builds in flight).
- Full 565-asset install sweep.
- Verify final relay uploads (Packages + gpkg + commits.json) for the new
  builds.
