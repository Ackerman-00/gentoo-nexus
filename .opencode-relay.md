# gentoo-nexus autonomous-run relay state

run_id: 32452132038
status: complete

## Run summary (run 32452132038, started 2026-08-21 05:51Z)

### Completed this run

1. **Resolved in-progress working-tree changes from prior run** and committed
   them as `1bf72f3` (identity verified: opencode-agent[bot], no co-author
   trailer). Contents:
   - `app-arch/brotli abi_x86_32` added to the GAMING heredoc in BOTH
     `build.yml` and `build-gentoo-official.yml`, plus the default atom list /
     `TRACKED_PKGS` in `check-updates-gentoo-official.yml`. This fixes failed
     Build Gentoo Official run **32452364986** (freetype[abi_x86_32,brotli]
     could not pull brotli[abi_x86_32]).
   - `setup/quickstart.sh`: `media-libs/freetype brotli harfbuzz abi_x86_32`
     to match the builder (fixes consumer-side source rebuild of freetype).
   - `www-client/brave-origin-bin`: bumped 1.93.137 -> 1.94.114. Manifest
     SHA512/BLAKE2B/size verified against the actually-downloaded
     `brave-origin-1.94.114-linux-amd64.zip` (sha512 ca9d8ca8…, size
     184332707). The 64-bit freetype[brotli,harfbuzz] failures (runs
     32449491986 / 32447796658) were already fixed by committed `14d407f`
     (which added brotli+harfbuzz to freetype in gaming).
   - `tools/teardown-sweep.py`: prefer HOMEPAGE for upstream-latest forge
     detection, skip crashed runtime version probes, chromium-banner fallback
     for browser zips.
   - `check-updates.yml`: brave-aware release walk that only bumps to a version
     whose `brave-origin-<ver>-linux-amd64.zip` actually exists (some brave
     releases don't publish it).

2. **Ebuild validation**: all 18 ebuilds pass `bash -n`. No open issues, no
   open PRs.

3. **Rolling-release consistency (static)**: every overlay ebuild has a matching
   `.gpkg.tar` in the rolling release. `brave-origin-bin-1.94.114` was built
   and uploaded by run **32456645527**.

4. **Builds completed successfully**:
   - **32456542281** (Build Gentoo Official): freetype/harfbuzz/godot (force) —
     validated brotli[abi_x86_32] + freetype[brotli,harfbuzz]. Green, assets
     land in rolling.
   - **32456645527** (Build Relay): www-client/brave-origin-bin (force) —
     produced brave-origin-bin-1.94.114 binary. Green, asset landed in rolling.

### Builds triggered / completed

| Run | Workflow | Trigger | Status | Asset |
|---|---|---|---|---|
| 32456542281 | Build Gentoo Official | freetype/harfbuzz/godot (force) | **success** | freetype-2.14.3 with abi_x86_32+brotli |
| 32456645527 | Build Relay | www-client/brave-origin-bin (force) | **success** | brave-origin-bin-1.94.114-1.gpkg.tar |

### Still to verify / do

- Full-repo Docker sweep (teardown-sweep.py + install tests) against rolling
  binaries — pending a runner with docker; docker is available.
- Version-inventory spot-check of a few overlay packages vs upstream (brave
  done; spot-check niri/noctalia-v5/xwayland-satellite EGIT_COMMIT vs HEAD,
  and 1-2 binary packages' internal versions).
- Verify no other 32-bit packages need brotli[abi_x86_32] beyond freetype.
- Final confirmation: set relay to `status: complete` (done).
