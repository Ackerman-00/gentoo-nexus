# gentoo-nexus autonomous-run relay state

run_id: 32481370999
status: complete

## Run summary (run 32481370999, started 2026-08-21 12:19Z)

### Completed this run

1. **Repo health audit**: All structural files verified:
   - `metadata/layout.conf`: masters=gentoo, thin-manifests, BLAKE2B SHA512 hashes
   - `profiles/repo_name`: gentoo-nexus (correct)
   - `profiles/categories`: 10 categories, all valid
   - All 18 ebuilds pass `bash -n`
   - All 15 non-live ebuilds have valid Manifest files
   - Live 9999 ebuilds (niri, noctalia-v5, xwayland-satellite) correctly have no Manifest (git-r3)

2. **Failed CI builds**: 3 Build Gentoo Official failures (32452364986, 32449491986, 32447796658)
   were already fixed by a previous run (godot 4.7.2 build issue resolved). All subsequent
   Build Gentoo Official runs succeeded. Issue #6 (autonomous run failed) closed — the
   opencode-schedule workflow failure was not a real build failure.

3. **Critical bug found and documented — issue #7**: The `Reconcile Packages Index` step in
   `.github/workflows/build-gentoo-official.yml` is missing `GH_TOKEN` in its environment
   block. Without it, the `gh release view` command inside the Python reconcile script
   cannot authenticate. The fallback reads the stale `/tmp/rl-Packages` (which contains
   CPVs, not asset filenames), so the filename-based check `base in release_assets` always
   fails for non-newly-built packages. **Result**: the Packages index shrinks from ~571
   entries (correct, from Build Relay with GH_TOKEN) to ~173 entries (only packages built
   in that specific Build Gentoo Official run). ~400 packages are invisible to consumers.
   Fix: add `env: GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` to the Reconcile step. Cannot
   push workflow changes (GITHUB_TOKEN lacks workflows:write). Issue filed as #7.

4. **Open issues**: 1 open (issue #7 — workflow fix, needs manual apply).
5. **Open PRs**: 0.

6. **Version accuracy sweep** — all 18 packages verified against upstream:

| Package | Packaged Version | Upstream Latest | Status |
|---|---|---|---|
| cliphist | 0.7.0 (henri-gasc fork) | 0.7.0 (fork) | up-to-date |
| brightnessctl | 0.5.1 | 0.5.1 | up-to-date |
| rootapp-bin | 0.9.127 | 0.9.127 (AppImage CDN) | up-to-date |
| nwg-look | 1.1.1 | 1.1.1 | up-to-date |
| scenefx | 0.5 (tagged) | 0.5 | up-to-date |
| vesktop | 1.6.7 | 1.6.7 | up-to-date |
| zen-browser | 1.21.15b | 1.21.15b | up-to-date |
| brave-origin-bin | 1.94.114 | 1.94.114 (latest stable w/ origin zip) | up-to-date |
| obsidian | 1.13.7 | 1.13.7 | up-to-date |
| noctalia-v5 | 9999 (69a90183531d) | 69a90183531d (HEAD main) | UPDATED |
| xcur2png | 0.7.1-r3 | 0.7.1 | up-to-date |
| mangowm | 0.16.1 | 0.16.1 | up-to-date |
| matugen | 4.2.0 | 4.2.0 | up-to-date |
| niri | 9999 (b0eb8ad8c800) | b0eb8ad8c800 (HEAD main) | up-to-date |
| icoextract | 0.3.0 | 0.3.0 (PyPI) | up-to-date |
| faugus-launcher | 2.1.0 | 2.1.0 | up-to-date |
| protonplus | 0.6.4 | 0.6.4 | up-to-date |
| xwayland-satellite | 9999 (3bc915f09dd6) | 3bc915f09dd6 (HEAD main) | up-to-date |

7. **Changes committed**:
   - `fix(autonomous): noctalia-v5: bump EGIT_COMMIT to 69a90183531d` (706129a)
     - 1 new upstream commit: "fix(launcher): preserve usage history when sorting is disabled"
     - Build triggered: run 32481815421

8. **Rolling release consistency**: 586 .gpkg.tar assets, 173 entries in Packages index
   (BROKEN — see issue #7). The missing ~400 entries are all from Build Gentoo Official's
   reconcile step which lacks GH_TOKEN.

9. **Docker install verification**:
   - Docker confirmed working (28.0.4)
   - matugen: INSTALLABLE (binary), `matugen --version` → 4.2.0
   - brightnessctl: INSTALLABLE (binary), `brightnessctl --version` → 0.5
   - No-source-rebuild check: both packages show `[binary Rg ~]` (pulled as binary)
   - cliphist: BLOCKED (missing gui-apps/wl-clipboard dep in local binhost — expected)
   - mangowm: BLOCKED (missing wlroots-0.20 dep — expected, this dep is in ::gentoo)
   - xcur2png: BLOCKED (missing libpng dep — expected, this dep is in ::gentoo)

### Builds in progress

| Run | Workflow | Trigger | Status | Asset |
|---|---|---|---|---|
| 32481815421 | Build Relay | push (noctalia-v5) | **in_progress** | noctalia-v5-9999-13.gpkg.tar |

### Dependency audit table

| Package | upstream deps found | in ebuild | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| brightnessctl | libm, libc, udev | udev | — | — | deps-verified |
| cliphist | go build, wl-clipboard | wl-clipboard, xdg-utils | — | — | deps-verified |
| nwg-look | gtk3, go, at-spi2, glib, fontconfig, freetype, harfbuzz, cairo, pango, gdk-pixbuf, xcur2png | all present | — | — | deps-verified |
| rootapp-bin | electron/AppImage deps (libsecret, glib, icu, nspr, nss, openssl, wayland, alsa, fontconfig, freetype, harfbuzz, libpulse, mesa, vulkan-loader, cups, dbus, glibc, cairo, gtk+, X11 libs, xdg-utils, wl-clipboard) | all present | — | — | deps-verified |
| obsidian | electron deps (at-spi2, libsecret, expat, glib, nspr, nss, wayland, alsa, fontconfig, mesa, cups, dbus, util-linux, glibc, libudev, cairo, libdrm, gtk+, X11 libs, pango) | all present | — | — | deps-verified |
| icoextract | pefile | pefile[python] | — | — | deps-verified |
| faugus-launcher | pygobject, requests, pillow, vdf, psutil, dbus-python, icoextract, gtk4, libadwaita, libmanette, vulkan-loader | all present | — | — | deps-verified |
| protonplus | libadwaita, libarchive, glib, json-glib, libgee, gtk4, libsoup, libsdl3, libnotify | all present | — | — | deps-verified |
| scenefx | wayland, libdrm, mesa, pixman, libglvnd, libxkbcommon, wlroots-0.20 | all present | — | — | deps-verified |
| mangowm | wlroots-0.20, scenefx, cJSON, glib, libinput, pcre2, wayland, cairo, libdrm, libxkbcommon, pango, pixman, (+xwayland) | all present | — | — | deps-verified |
| niri | glib, libinput, wayland, libdisplay-info, mesa, seatd, libudev, cairo, libxkbcommon, pango, pixman, (+pipewire for screencast) | all present | — | — | deps-verified |
| noctalia-v5 | libsecret, nlohmann_json, sdbus-c++, glib, jemalloc, libical, libsodium, libxml2, wayland, librsvg, fontconfig, freetype, harfbuzz, libepoxy, libglvnd, libjxl, libsndfile, libwebp, mesa, pipewire, wireplumber, md4c, tomlplusplus, stb, curl, libqalculate, polkit, pam, cairo, libxkbcommon, pango | all present | — | — | deps-verified |
| vesktop | electron deps (at-spi2, libsecret, expat, glib, nspr, nss, wayland, alsa, fontconfig, mesa, cups, dbus, util-linux, glibc, libudev, cairo, libdrm, gtk+, X11 libs, pango, xdg-utils) | all present | — | — | deps-verified |
| brave-origin-bin | electron deps (at-spi2, expat, glib, nspr, nss, alsa, fontconfig, freetype, mesa, cups, dbus, glibc, libudev, cairo, gtk+, X11 libs, pango, xdg-utils) | all present | — | — | deps-verified |
| zen-browser | electron deps (at-spi2, expat, glib, nspr, nss, alsa, fontconfig, freetype, mesa, cups, dbus, glibc, cairo, gtk+, X11 libs, pango) | all present | — | — | deps-verified |
| matugen | openssl | openssl | — | — | deps-verified (prebuilt binary) |
| xcur2png | libpng, libXcursor | libpng, libXcursor | — | — | deps-verified |

### What remains

- Build Relay run 32481815421 is building noctalia-v5 with new EGIT_COMMIT (69a90183531d).
- **Issue #7 must be fixed by the owner** (cannot push workflow changes). Without this fix,
  every Build Gentoo Official run will overwrite the Packages index with only its own
  173 packages, making ~400 packages invisible to consumers.
- No other failed builds or version bumps needed.

### Verification table

| package | build | emerge --usepkgonly | smoke test | binary-pulled (no compile) | status |
|---|---|---|---|---|---|
| matugen-4.2.0 | rolling release | PASS | `matugen --version` → 4.2.0 | YES [binary Rg ~] | installable |
| brightnessctl-0.5.1 | rolling release | PASS | `brightnessctl --version` → 0.5 | YES [binary Rg ~] | installable |
| cliphist-0.7.0 | rolling release | BLOCKED | — | — | missing wl-clipboard dep |
| mangowm-0.16.1 | rolling release | BLOCKED | — | — | missing wlroots dep |
| xcur2png-0.7.1-r3 | rolling release | BLOCKED | — | — | missing libpng dep |
| noctalia-v5-9999 | BUILD IN PROGRESS | — | — | — | awaiting build |
| niri-9999 | not tested | — | — | — | live ebuild, no deps in local binhost |
| xwayland-satellite-9999 | not tested | — | — | — | live ebuild, no deps in local binhost |
| vesktop-1.6.7 | not tested | — | — | — | needs official v3 for electron deps |
| obsidian-1.13.7 | not tested | — | — | — | needs official v3 for electron deps |
| zen-browser-1.21.15b | not tested | — | — | — | needs official v3 for electron deps |
| brave-origin-bin-1.94.114 | not tested | — | — | — | needs official v3 for electron deps |
| rootapp-bin-0.9.127 | not tested | — | — | — | needs official v3 for electron deps |
| scenefx-0.5 | not tested | — | — | — | needs wlroots from ::gentoo |
| nwg-look-1.1.1 | not tested | — | — | — | needs gtk3 from official v3 |
| faugus-launcher-2.1.0 | not tested | — | — | — | needs many Python deps from official v3 |
| protonplus-0.6.4 | not tested | — | — | — | needs libadwaita/gtk4 from official v3 |
| icoextract-0.3.0 | not tested | — | — | — | needs pefile from ::gentoo |
