# gentoo-nexus autonomous-run relay state

status: in_progress
run_id: 32203418035

## Run summary (current run, started 2026-08-19 ~01:00 UTC)

### BLOCKER — workflow fixes verified but pushable only by owner (confirmed this run)

The single root cause of the recurring CI failures is **confirmed and the fix
re-verified with fresh docker evidence this run**:

- Portage `_populate_local()` (bintree.py, portage-3.0.81 in
  gentoo/stage3:amd64-desktop-openrc) **silently skips multi-instance gpkg
  files** (BUILD_ID > 0) unless they live in `<pkgdir>/<category>/<pn>/`
  subdirectories.
- The current "Restore GPKG Layout" step in both build.yml and
  build-gentoo-official.yml places files in flat `<category>/` dirs →
  `emaint binhost --fix` regenerates an **empty Packages index (PACKAGES: 0)**
  → whole nexus binrepo invisible → source rebuilds → gpm/ncurses circular
  deps and `no ebuilds built with USE flags to satisfy rust-bin[abi_x86_32]`.

**Fresh docker proof (this run):**
- Flat `category/` layout: `emaint binhost --fix` → **0 CPVs**.
- `category/pn/` layout: same two files → **2 CPVs indexed**
  (gpm-1.20.7-r6, rust-bin-1.95.0).

**Fix was authored and tested this run, then the push was attempted and
rejected** (GitHub App token has no `workflows` scope):
`! [remote rejected] main -> main (refusing to allow a GitHub App to create
or update workflow ... without workflows permission)`.

**Fallback executed:** full patch (layout fix + broadened heal trigger) posted
to issue #5 as comments 5336304243 + 5336304787 (with both diffs). **Owner
must apply from a local checkout** (as with b467d91). Patch files saved at
`/tmp/opencode/build-fix.patch`, `/tmp/opencode/bgo-fix.patch`.

### rust-bin abi_x86_32 (verified this run)

`dev-lang/rust-bin` DOES inherit multilib-minimal and the release
`rust-bin-1.95.0-1.gpkg.tar` carries `abi_x86_32` (metadata/USE checked).
AGENTS.md "inert flags" claim is stale. However the workflows do NOT need a
separate `dev-lang/rust-bin abi_x86_32` line because the desktop-openrc
profile's multilib ABI_X86 default already enables it; the release binary
proves it. quickstart.sh lines 334-335 keep the consumer lines (harmless).

### CI status

- Build Gentoo Official 32202901635 FAILED (gpm/ncurses circular deps on
  `=sys-libs/gdbm-1.26` — ncurses source-built because the nexus binrepo is
  invisible; exact root cause above). Newer run 32204474513 in_progress.
  Recent runs alternating success/failure (failures = empty-index root cause).
- Check Updates all green.

### Version sweep (2026-08-19, verified via GitHub releases API)

- **vesktop 1.6.6 → 1.6.7** was OUTDATED; bumped + Manifest hashes verified
  from downloaded .deb files + md5-cache regenerated (commit 5d9ce36, pushed).
- **niri pin 606284464d4a → e9b215fef4b1** (HEAD 2026-08-18) stale, bumped.
- **noctalia pin 74db8e7… → fa1a968cf730** (HEAD 2026-08-19) stale, bumped.
- xwayland-satellite pin 3bc915f09dd6 == HEAD (in sync).
- All other 14 ebuilds: latest versions confirmed (brightnessctl 0.5.1,
  cliphist 0.7.0, nwg-look 1.1.1, rootapp-bin 0.9.126 [hash matches live
  AppImage], obsidian 1.13.7, icoextract 0.3.0, faugus-launcher 2.1.0,
  protonplus 0.6.4, scenefx 0.5, mangowm 0.16.1, brave-origin 1.93.136,
  zen 1.21.14b, matugen 4.2.0, xcur2png 0.7.1).

### Dependency deep-audit (2026-08-19, emerge -p graphs, desktop/gnome profile)

All 18 ebuilds: `emerge -p` resolves cleanly (graph sizes 1–65 pkgs). Only
exception: noctalia-v5 + xwayland-satellite hit a perl 5.42 (preinstalled in
the stage3 IMAGE) vs 5.44 (current tree) transition conflict — purely
environmental (installed virtual/perl-* pinning =perl-5.42*), NOT an ebuild
dep issue; a consumer on current perl resolves fine. niri graph = 65 pkgs OK.

### Issues / PRs

- #5: comments updated this run (5336304243, 5336304787) with verified fix +
  full diffs. Owner action required.
- No open PRs.

## Remaining work this run

1. Full-repo docker sweep against the DOWNLOADED rolling release binaries
   (layout workaround: place downloaded gpkg assets into category/pn/ dirs
   locally so the binrepo is visible — mirrors the fix the owner will apply).
   Downloads in progress (303/553 .gpkg.tar fetched so far).
2. Install-verification + no-source-rebuild checks for as many assets as
   time allows; ledger below.
3. Final relay update (status complete) + run_id.

## Install-test coverage ledger

| package | last install-tested | result | smoke |
|---|---|---|---|
| (to be filled this run) | | | |