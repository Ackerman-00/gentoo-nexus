# gentoo-nexus autonomous-run relay state

run_id: 32611587978
status: unfinished

## Run 32611587978 (2026-08-23) — IN PROGRESS

Work plan (priority order):
1. [x] build.yml control-plane fix from issue #17 APPLIED + VALIDATED locally
   (YAML OK, pn_from_cpv 9/9) but PUSH BLOCKED: git push rejected with
   "refusing to allow a GitHub App to create or update workflow
   `.github/workflows/build.yml` without `workflows` permission"; API PUT
   contents also 403 "Resource not accessible by integration". The owner's
   2026-08-23 grant is NOT in effect for this run's token. NOT retried
   blindly per fallback rule. Patch stays ready in issue #17 (verified it
   matches current main). Re-try on a future run if the App gains
   workflows:write; otherwise owner applies issue #17 diff manually.
2. [ ] Prune 39 stale rolling assets (32 PNs with >1 versions; list computed
   in /tmp/opencode/stale_assets.txt), reconcile Packages index after.
3. [ ] Verify failed runs 32543610884 (Build Relay) & 32452364986 (Build
   Gentoo Official) are superseded by newer green builds of same packages.
4. [ ] Full version sweep of all 20 ebuilds vs upstream (gh api releases).
5. [ ] Dependency deep audit (20 packages) + official-atom gpkg teardown.
6. [ ] Docker battle tests (install + no-source-rebuild + CLI/setup).
7. [ ] Issue #17 reply/close after fix verified in a real build.
8. [ ] Pre-completion gate + final matrix.

---
# Previous run record (kept for context)

run_id: 32593909044
status: complete

## Run summary (32593909044, 2026-08-22) — binhost consumer-path fully repaired

### Root causes found & fixed today (every fix verified before/after in clean containers)

1. **niri-9999 / xwayland-satellite-9999 masked for every consumer** — commit 222e249.
   niri ebuild had NO KEYWORDS line; satellite had KEYWORDS="". Both were "masked by
   missing keyword" in emerge AND their already-shipped rolling binaries recorded no
   KEYWORDS (binpkg masked too -> silent source-build fallback / usepkgonly hard fail).
   Evidence: `gh run view` + container: "All ebuilds that could satisfy gui-wm/niri have
   been masked ... (masked by: missing keyword)"; index KEYWORDS: None for both entries.
   FIX: KEYWORDS="~amd64" on both ebuilds + rebuild via Build Relay **32597964589**
   (SUCCESS) and chained relay **32599467405** (SUCCESS): shipped niri-9999-4 +
   xwayland-satellite-9999-4, both now indexed with KEYWORDS='~amd64' + full DEPEND.

2. **gpm<->ncurses circular-deps cycle blocking ALL 6 Electron atoms** (obsidian,
   vesktop, zen-browser, brave-origin-bin, rootapp-bin, protonplus). TRUE mechanism
   (proven, replaces yesterday's theory): stage3 ships gpm/ncurses; today's profile
   defaults ncurses[gpm] ON + readline's hard abi_x86_32 chain forces a simultaneous
   USE-flag reinstall of BOTH sys-libs pair; each binary requires the other at runtime
   (slot-op) -> portage refuses ordering. The previous run's `-abi_x86_32` pins were
   DEAD LETTERS (overridden by readline's requirement). Bootstrap attempts with plain
   `-1` were no-ops (same version installed). WORKING FIX (commit 222e249 quickstart +
   proven in containers): `emerge --getbinpkg --usepkgonly --nodeps sys-libs/gpm`
   BEFORE the main install. After: rootapp pretend went rc=1 circular -> rc=0.

3. **Binpkg USE-parity drift** (profile defaults flipped after builds): glib/pango/
   mesa/gtk+ built WITH `sysprof`, pipewire WITH `gstreamer`, libxmlb WITH
   `introspection`, git WITH `keyring`, gtk+ WITH `colord` — all rejected by fresh
   trees under --binpkg-respect-use=y -> source recompiles. FIX: parity pins appended
   to machine/package.use (222e249). Symptom string captured BEFORE:
   "!!! The following binary packages have been ignored due to non matching USE:
   =dev-libs/glib-2.88.3 sysprof / =media-video/pipewire-1.6.8 gstreamer"; AFTER:
   zero ignored lines, all [binary].

4. **acct-group/seat unshippable**: build.yml deliberately strips acct-* from the
   Packages index AND official-v3 host ships 208 acct-group pkgs but NOT seat ->
   '[ebuild] acct-group/seat' + strict --usepkgonly FAIL ("there are no binary packages
   to satisfy"). FIX (d78521e): quickstart pre-installs it from source (metadata-only
   groupadd, ~1s) so no later transaction ever schedules it. Agent-side stopgap also
   applied: seat-0-r1-1 entry re-added to remote index (uploaded 21:40Z, verified
   remotely = 578 CPVs). Root-fix request posted on issue #16 (comment
   issuecomment-5382797668): delete the acct-* awk strip; workflow push remains blocked
   for this token (attempted previously, run 32584675904 documents the rejection).

5. **Packages index health re-verified from the assets themselves**: streamed each
   gpkg's embedded metadata via HTTP range requests (/tmp/opencode tooling) — ALL 577
   entries byte-match their binary's recorded metadata (USE/IUSE/SLOT/EAPI/DEPEND/
   RDEPEND/LICENSE/KEYWORDS/CFLAGS...), 0 mismatches, 0 dangling entries. Yesterday's
   dep repair had survived pipeline regenerations; no upload churn needed beyond seat.

### FINAL FULL-MATRIX CONSUMER SIMULATION (fresh gentoo/stage3:amd64-desktop-openrc,
remote nexus+official-v3 binrepos, machine/make.conf + package.use, webrsync today)
21/21 atoms rc=0 with ZERO [ebuild] lines — overlay 18/18 + mesa + gentoo-kernel + llvm:

| atom | rc | binary | ebuild |
|---|---|---|---|
| brightnessctl/cliphist/nwg-look/matugen/xcur2png/icoextract | 0 | 1–2 | 0 |
| rootapp-bin | 0 | 78 | 0 |
| obsidian | 0 | 18 | 0 |
| faugus-launcher | 0 | 24 | 0 |
| protonplus | 0 | 135 | 0 |
| scenefx | 0 | 86 | 0 |
| mangowm | 0 | 88 | 0 |
| niri | 0 | 66 | 0 |
| noctalia-v5 | 0 | 60 | 0 |
| vesktop | 0 | 22 | 0 |
| brave-origin-bin | 0 | 22 | 0 |
| zen-browser | 0 | 12 | 0 |
| xwayland-satellite | 0 | 65 | 0 |
| media-libs/mesa | 0 | 25 | 0 |
| sys-kernel/gentoo-kernel | 0 | 12 | 0 |
| llvm-core/llvm | 0 | 5 | 0 |

### Install-test table (real `emerge --getbinpkg --usepkgonly`, clean containers)

| package | build | emerge --usepkgonly | smoke test | status |
|---|---|---|---|---|
| niri-9999 (-4 NEW) | 32597964589 | INSTALLED (66 bins incl. seatd+seat chain) | `niri 26.04 (dd75865)` runs; ldd 0 not-found | installable |
| xwayland-satellite-9999 (-4 NEW) | 32599467405 | INSTALLED | binary present, --version ok | installable |
| mangowm-0.16.1-3 | 32588838250 | INSTALLED | ldd 0 not-found | installable |
| noctalia-v5-9999-3 | 32581560736 | INSTALLED | `Usage: noctalia <command>` runs; ldd 0 | installable |
| scenefx-0.5-4 | 32588838250 | INSTALLED | installed as wlroots dep chain | installable |
| obsidian-1.13.7-2 | rolling | INSTALLED | installed | installable |
| brightnessctl-0.5.1 | rolling | INSTALLED | `brightnessctl --version` -> 0.5; equery check failed(0)=PASS; belongs OK; ldd 0 | installable |
| cliphist-0.7.0 | rolling | INSTALLED | CLI usage prints | installable |
| matugen-4.2.0 | rolling | INSTALLED | `matugen 4.2.0` | installable |

### Version inventory sweep (18/18, gh api + artifact tear, 2026-08-22)

| package | packaged | upstream latest | status |
|---|---|---|---|
| brightnessctl | 0.5.1 | 0.5.1 | up-to-date |
| cliphist | 0.7.0 | v0.7.0 | up-to-date |
| nwg-look | 1.1.1 | v1.1.1 | up-to-date |
| rootapp-bin | 0.9.127 | internal X-AppImage-Version=0.9.127 (artifact republished upstream; Manifest BLAKE2B cb23b4d2…/SHA512 cd43adbc… MATCH new 392239608-byte file) | up-to-date |
| obsidian | 1.13.7 | v1.13.8 exists but ships ONLY .apk (no Linux assets yet); updater guard handles this exactly (check-updates.yml walks releases newest-first requiring desktop assets) | up-to-date (newest installable) |
| icoextract | 0.3.0 | 0.3.0 | up-to-date |
| faugus-launcher | 2.1.0 | 2.1.0 | up-to-date |
| protonplus | 0.6.4 | v0.6.4 | up-to-date |
| scenefx | 0.5 | tag 0.5 | up-to-date |
| mangowm | 0.16.1 | 0.16.1 | up-to-date |
| niri (live) | pin dd75865f547f | HEAD == dd75865f547f | up-to-date |
| noctalia-v5 (live) | pin 7fd9f7bfa960 | HEAD == 7fd9f7bfa960 | up-to-date |
| xwayland-satellite (live) | pin 3bc915f09dd6 | HEAD == 3bc915f09dd6 | up-to-date |
| vesktop | 1.6.7 | v1.6.7 | up-to-date |
| brave-origin-bin | 1.94.114 | newest tag shipping non-beta linux zip IS v1.94.114 (v1.95.x = beta-only origin assets) | up-to-date |
| zen-browser | 1.21.15b | 1.21.15b | up-to-date |
| matugen | 4.2.0 | v4.2.0 | up-to-date |
| xcur2png | 0.7.1-r3 | 0.7.1 | up-to-date |

### Dependency audit table (all 18, re-derived this run)

| Package | upstream deps found | in ebuild | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| brightnessctl | libm/libc/udev | udev | — | — | deps-verified |
| cliphist | go stdlib, wl-clipboard runtime | wl-clipboard, xdg-utils | — | — | deps-verified |
| nwg-look | gtk3/glib/fontconfig stack + xcur2png | all present | — | — | deps-verified |
| rootapp-bin | electron bundle deps (QA_PREBUILT) | all present | — | — | deps-verified |
| obsidian | electron deps (QA_PREBUILT) | all present | — | — | deps-verified |
| icoextract | pefile | pefile | — | — | deps-verified |
| faugus-launcher | pygobject/requests/pillow/vdf/psutil/dbus-python/icoextract/gtk4/libadwaita/libmanette/vulkan-loader | all present | — | — | deps-verified |
| protonplus | libadwaita/libarchive/glib/json-glib/libgee/gtk4/libsoup/libsdl3/libnotify | all present | — | — | deps-verified |
| scenefx | wayland/libdrm/mesa/pixman/libglvnd/libxkbcommon/wlroots-0.20 | all present | — | — | deps-verified |
| mangowm | wlroots-0.20/scenefx/cJSON/glib/libinput/pcre2/wayland/cairo/libdrm/libxkbcommon/pango/pixman/linux-headers/xwayland(X) | all present | — | — | deps-verified |
| niri | glib/libinput/wayland/libdisplay-info<0.4(mirrors Cargo.toml 0.3.x @pin AND @HEAD)/mesa/seatd/libudev/cairo/xkbcommon/pango/pixman/pipewire(screencast)+xdg-desktop-portal-gnome | all present; recorded==evaluated (diffed binary metadata vs ebuild) | — | — | deps-verified |
| noctalia-v5 | libsecret/nlohmann_json/sdbus-c++/glib/jemalloc(+)/libical/libsodium/libxml2/wayland/librsvg/fontconfig/freetype/harfbuzz/libepoxy/libglvnd/libjxl/libsndfile/libwebp/mesa/pipewire/wireplumber/md4c/tomlplusplus/stb/curl/libqalculate/polkit/pam/cairo/xkbcommon | all present | — | — | deps-verified |
| vesktop | electron deps (QA_PREBUILT) | all present | — | — | deps-verified |
| brave-origin-bin | chrome deps (QA_PREBUILT) | all present | — | — | deps-verified |
| zen-browser | chrome deps (QA_PREBUILT) | all present | — | — | deps-verified |
| matugen | openssl | openssl | — | — | deps-verified |
| xcur2png | libpng/libXcursor | libpng, libXcursor | — | — | deps-verified |
| xwayland-satellite | wayland/libxcb/xcb-util-cursor/xwayland | all present | — | — | deps-verified |

### Fresh evidence strings (2026 h. checks, THIS run, with results)

- pkgcheck scan PASS: dev-util/pkgcheck + pkgdev installed in clean
  gentoo/stage3:amd64-desktop-openrc; `pkgcheck scan` over the overlay returned
  RC=0. Findings fixed in f31e80e: upstream remote-ids for all 18 packages,
  obsidian Manifest regenerated (DIST lines lacked SIZE -> "invalid data
  format"; now full BLAKE2B+SHA512+SIZE for all 3 distfiles incl. arm64 via
  `pkgdev manifest`), brave empty DEPEND="" removed, matugen S-order fixed.
  Post-fix targeted-finding count: 0 MissingRemoteId/EmptyGlobalAssignment/
  VariableOrderWrong/InvalidManifest. Accepted-and-named remainder:
  WhitespaceFound cosmetics on live ebuilds, UnknownRestrict network-sandbox,
  VisibleVcsPkg (pinned-9999 model), musl NonsolvableDepsInDev for -bin pkgs,
  UnknownCategoryDirs (repo tool dirs). bash -n PASS on all 18 ebuilds;
  EAPI=8 present in 18/18.
- emerge --pretend PASS: full-matrix above — `emerge --pretend --verbose --getbinpkg`
  21/21 rc=0, ZERO [ebuild], zero "ignored due to" lines in final pass.
- equery PASS: `equery check app-misc/brightnessctl` -> "failed(0)" (=0 failures),
  exit 0; `equery belongs /usr/sbin/brightnessctl` -> correct attribution.
- DOCKER BATTLE TEST PASS: every result above is docker run --rm
  gentoo/stage3:amd64-desktop-openrc with REMOTE binrepos; ldd not-found count = 0 for
  niri/mangowm/noctalia/brightnessctl; tools/nexus --help + list PASS inside container;
  setup/quickstart.sh bash -n PASS.
- Index↔asset coherence: 577/577 entries byte-match gpkg-embedded metadata (range-
  request streaming verifier, 0 mismatches).

### Multilib (32-bit) integrity

All 136 abi_x86_32 pins from machine/package.use checked against newest rolling
entries: 135 ship BOTH ABIs; virtual/zlib provided by official-v3 ([binary Rg]
virtual/zlib ABI_X86="32* (64)" observed in sweep). No gaps.

### Commits pushed this run (all opencode-agent[bot], zero trailers)

- 222e249 fix(autonomous): unmask live-9999 binpkgs + USE-parity pins + gpm bootstrap
- e205b12 docs(autonomous): troubleshooting for gpm cycle + USE-parity drift
- d78521e fix(autonomous): quickstart pre-installs acct-group/seat
- Builds triggered & green: Build Relay 32597964589 (niri), chained 32599467405
  (xwayland-satellite); assets niri-9999-4 / xwayland-satellite-9999-4 in rolling.
- Issue #16 updated with fresh evidence + exact patch locations for owner
  (workflow pushes remain token-blocked).

### Pre-completion gate

1. git status clean after final commit: YES (checked before push)
2. Touched packages install-tested in CLEAN containers incl. fresh-checker rerun: YES
   (final_check.sh = brand-new container post-everything; table above)
3. Ledger updated: YES (this file)
4. Dep audit covers 18/18: YES
5. Every claim backed by command output in transcript: YES
6. NOT verified / named gaps: (a) pkgcheck cosmetic whitespace findings accepted
   (named in f31e80e message); (b) runtime smoke of GUI compositors impossible headless — used --version/--help
   per rules; (c) acct-group/seat will vanish from index again on next Build Relay
   chain until owner removes the awk strip (stopgap re-add documented); (d) builder
   side of parity pins (adding same flags to build.yml base block) pending owner;
   consumer pins keep consumers green regardless.
7. Relay contains run_id 32593909044 + status complete: YES (this file)

### NEXT-RUN PRIORITY

1. If Build Gentoo Official ran: verify index kept deps (META_KEYS patch still
   pending owner) — re-run agent-side repair if entries went dep-blind again.
2. After ANY Build Relay chain: re-add acct-group/seat entry (tooling pattern:
   stream seat-0-r1-1.gpkg.tar metadata, insert block, upload Packages+.gz LAST)
   OR better: owner removes the acct-* strip (issue #16 comment has exact line).
3. Watch ::gentoo profile flips (sysprof/gstreamer-class drift): symptom =
   "ignored due to non matching USE" in consumer sims; remedy = extend
   machine/package.use parity block (+ builder mirror when workflows editable).
4. obsidian: bump to 1.13.8 once upstream publishes Linux deb/tar.gz assets
   (updater will handle automatically; SRC_URI pattern already correct).
