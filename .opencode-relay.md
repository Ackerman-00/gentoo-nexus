# gentoo-nexus autonomous-run relay state

run_id: 32575880120
status: unfinished

## Run summary (run 32575880120, 2026-08-22) — FINAL PHASE

### Root causes found & fixed today (all verified with command output)

1. **Consumer configs rejected builder binpkgs (USE mismatch)** — commits 444eb08,
   89b01c3, then corrected by 08366ff:
   - rolling binhost builds ~139 core atoms WITH abi_x86_32; machine/package.use +
     quickstart defaults did not → --binpkg-respect-use rejected those binaries.
   - First fix shipped gaming list only → REGRESSED (missing builder base block
     `sys-libs/ncurses gpm` etc.) → ncurses[gpm,32] rebuild hit circular dep.
   - Final: machine/package.use mirrors builder base+gaming EXACTLY minus rust/gpm.

2. **gpm<->ncurses slot-op cycle** — commit 321c5b3: dropped consumer-side gpm +
   ncurses-gpm entries. Consumer ncurses now matches official v3 instance
   [abi_x86_32 cxx stack-realign tinfo -gpm]; nothing desktop pulls gpm; steam block
   keeps it.

3. **Stale shipped binaries** — mangowm-0.16.1-1 (IUSE="X" vs repo "X asan") and
   noctalia-v5-9999-2 (IUSE="" vs "+jemalloc") were rejected by every consumer.
   Rebuilt via Build Relay 32579906503 + 32581560736 (both SUCCESS):
   mangowm-0.16.1-2 + noctalia-v5-9999-3 now in rolling; embedded-ebuild sweep of all
   18 assets: 16 MATCH pre-existing + these 2 fixed.

4. **Pipeline lost-update race** — Build Gentoo Official 32579769526 reverted fresher
   relay uploads (mangowm-2→-1, wlroots-5→-4, noctalia-3→-2 in index). FIX WRITTEN but
   NOT pushable (GITHUB_TOKEN lacks workflows scope): full patch filed in issue #16.

5. **Reconcile drops DEPEND/RDEPEND** (META_KEYS omission) → dep-blind Packages.gz →
   portage auto-enabled binpkg-changed-deps rejected EVERYTHING → whole binhost fell
   back to source builds. Agent-side repair uploaded 16:46Z(Packages)/17:13Z(Packages.gz)
   restored 383 DEPEND entries; matrix flipped back to [binary]. Workflow fix also in
   issue #16. STOPGAP: re-run /tmp/opencode/fix_index2.py after ANY future build until
   owner applies the patch.

6. **wlroots slot-op rebuild**: wlroots-0.20.2-5 recorded libdisplay-info:0/3= but
   consumers get 0.4.0 (0/4) → forced source rebuild of scenefx/mangowm chains.
   Rebuild dispatched: Build Relay **32587820306**
   (=gui-wm/mangowm-0.16.1 =gui-libs/scenefx-0.5 force_rebuild).

### Verification evidence (this run)

- Full-matrix sweep (18 atoms, quickstart-equivalent consumer sim, clean
  gentoo/stage3:amd64-desktop-openrc, remote nexus+official-v3 binrepos):
  BEFORE fixes: mass [ebuild] compiles + circular-dep errors.
  AFTER index repair + config fixes: compile=0 for 15/18; remaining =
  acct-group/seat (no binpkg exists anywhere; metadata-only pkg, no compilation) ×3,
  wlroots slot-op rebuild ×2 (rebuild in flight).
- Portage evidence strings captured: "binary packages ignored due to non matching USE",
  "ignored due to changed dependencies", "Error: circular dependencies" (before), and
  "[binary N g]" lines (after).
- tools/nexus --help/list PASS in container; quickstart bash -n PASS.
- Version sweep 18/18 up-to-date (gh api); rootapp AppImage hashes match Manifest;
  live pins == upstream HEAD.

### Remaining steps to complete this run

- [ ] Wait Build Relay 32587820306 → verify scenefx/wlroots/mangowm chain pulls [binary]
- [ ] RE-RUN index repair (fix_index2.py) + upload Packages & Packages.gz LAST
      (pipeline reconcile will have overwritten them dep-blind again)
- [ ] FINAL full-matrix sweep must be green (only acct-group/seat allowed as
      no-binpkg-exists exception)
- [ ] Install tests (--usepkgonly) + equery check + ldd + smoke for ≥5 diverse pkgs
      incl mangowm + noctalia-v5 new assets
- [ ] Update ledger/tables below; run tools/verify-gentoo.sh; flip status: complete

### Dependency audit table (all 18, re-derived 2026-08-22)

| Package | upstream deps found | in ebuild | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| brightnessctl | libm/libc/udev | udev | — | — | deps-verified |
| cliphist | go stdlib, wl-clipboard runtime | wl-clipboard, xdg-utils | — | — | deps-verified |
| nwg-look | gtk3, glib, fontconfig, freetype, harfbuzz, cairo, pango, gdk-pixbuf, xcur2png | all present | — | — | deps-verified |
| rootapp-bin | electron bundle deps | all present (QA_PREBUILT) | — | — | deps-verified |
| obsidian | electron deps | all present (QA_PREBUILT) | — | — | deps-verified |
| icoextract | pefile | pefile | — | — | deps-verified |
| faugus-launcher | pygobject, requests, pillow, vdf, psutil, dbus-python, icoextract, gtk4, libadwaita, libmanette, vulkan-loader | all present | — | — | deps-verified |
| protonplus | libadwaita, libarchive, glib, json-glib, libgee, gtk4, libsoup, libsdl3, libnotify | all present | — | — | deps-verified |
| scenefx | wayland, libdrm, mesa, pixman, libglvnd, libxkbcommon, wlroots-0.20 | all present | — | — | deps-verified |
| mangowm | wlroots-0.20, scenefx, cJSON, glib, libinput, pcre2, wayland, cairo, libdrm, libxkbcommon, pango, pixman, linux-headers, xwayland(X) | all present | — | — | deps-verified |
| niri | glib, libinput, wayland, libdisplay-info, mesa, seatd, libudev, cairo, libxkbcommon, pango, pixman, pipewire(screencast) | all present | — | — | deps-verified |
| noctalia-v5 | libsecret, nlohmann_json, sdbus-c++, glib, jemalloc(+), libical, libsodium, libxml2, wayland, librsvg, fontconfig, freetype, harfbuzz, libepoxy, libglvnd, libjxl, libsndfile, libwebp, mesa, pipewire, wireplumber, md4c, tomlplusplus, stb, curl, libqalculate, polkit, pam, cairo, libxkbcommon, pango | all present | jemalloc confirmed in rebuilt binpkg IUSE | — | deps-verified |
| vesktop | electron deps | all present (QA_PREBUILT) | — | — | deps-verified |
| brave-origin-bin | electron/chrome deps | all present (QA_PREBUILT) | — | — | deps-verified |
| zen-browser | electron/chrome deps | all present (QA_PREBUILT) | — | — | deps-verified |
| matugen | openssl | openssl | — | — | deps-verified |
| xcur2png | libpng, libXcursor | libpng, libXcursor | — | — | deps-verified |
| xwayland-satellite | wayland, libxcb, xcb-util-cursor, xwayland | all present | — | — | deps-verified |

### Commits pushed this run

- 444eb08 consumer multilib list v1 (superseded)
- 89b01c3 consumer FULL builder mirror (base+gaming)
- 414b825 relay progress
- 08366ff drop gpm/ncurses-gpm from consumer (cycle kill) [rebased 321c5b3]
- Issue #16: both workflow patches (merge-live + META_KEYS deps) for owner to apply
