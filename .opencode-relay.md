# gentoo-nexus autonomous-run relay state

status: in_progress
run_id: $RUN_ID

## This run's accomplishments

### Fixes committed and pushed
- **games-util/protonplus**: Added missing `>=media-libs/libsdl3-3.2` DEPEND (build failed
  without it — meson.build:36 ERROR: Dependency "sdl3" not found)
- **gui-wm/noctalia-v5**: Updated EGIT_COMMIT from stale `b46cdc1f2236` to upstream HEAD
  `ced2221ee68d`

### Ebuild verification (all 18 packages)
- 15 versioned ebuilds: all current against upstream releases
- 3 live ebuilds: EGIT_COMMIT pins verified (niri ✓, xwayland-satellite ✓, noctalia-v5 ✓ after fix)
- All 18 ebuilds pass `bash -n` syntax check

### Rolling release asset audit
**Up to date (11):** cliphist-0.7.0, brightnessctl-0.5.1, nwg-look-1.1.1, icoextract-0.3.0,
scenefx-0.5, vesktop-1.6.5, niri-9999, noctalia-v5-9999, xwayland-satellite-9999, matugen-4.1.0,
xcur2png-0.7.1-r3

**Stale — need rebuilds (7):**
- rootapp-bin: 0.9.125 → 0.9.126
- obsidian: 1.13.4 → 1.13.7
- faugus-launcher: 2.0.5 → 2.1.0
- protonplus: 0.5.22 → 0.6.4 (now with sdl3 dep fix)
- mangowm: 0.15.6 → 0.16.1
- brave-origin-bin: 1.93.129 → 1.93.136
- zen-browser: 1.21.10b → 1.21.14b

### Install test (clean stage3 Docker)
- matugen-4.1.0: ✅ binary install confirmed
- cliphist-0.7.0: ✅ binary install confirmed
- obsidian-1.13.4: ✅ binary install confirmed
- vesktop-1.6.5: ✅ binary install confirmed

## Known issues
- **Workflow files (.github/workflows/*) cannot be pushed** — GitHub App token lacks
  `workflows` permission. ncurses+gpm pre-build fix is locally saved but undeployed.
  Requires repo owner to push manually or grant workflows permission.
- **gpm/ncurses circular dep** persists without the workflow fix — after perl-cleaner
  rebuilds perl, gpm binary (runtime_slot_op on ncurses) conflicts with ncurses source
  rebuild (buildtime dep on gpm). The pre-build fix attempts to install both from binary
  before perl-cleaner, but if the pre-build fails silently (|| true), the cycle returns.
- **rootapp-bin version** — upstream (installer.rootapp.com) serves a static URL with no
  version in the filename. Ebuild 0.9.126 is the current latest (AppImage last-modified
  2026-08-05).

## Next actions needed
1. Trigger Build Relay for the 7 stale packages after current build (32006250263) completes
2. Repo owner must push workflow fix for ncurses+gpm circular dep
3. If the gpm/ncurses fix is pushed, re-run Build Relay to verify
