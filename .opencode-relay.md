# gentoo-nexus autonomous-run relay state

run_id: 32643573024
status: unfinished

## Run 32643573024 (2026-08-23) — IN PROGRESS CHECKPOINT

### Done this run (all pushed unless noted)
1. Issue #17 patch ATTEMPTED via git push per updated token grant: STILL BLOCKED
   (`refusing to allow a GitHub App to create or update workflow ... without workflows permission`).
   Patch validated locally (YAML safe_load OK; extracted build.sh passes bash -n;
   pn_from_cpv unit-tested vs 9 real CPVs). Delta beyond issue body posted as
   issue comment: perl pin ALSO needed in build-gentoo-official.yml + note that
   pn_from_cpv must include CATEGORY (virtual/libusb vs dev-libs/libusb both PN=libusb).
2. pkgcheck conformance batch pushed (commit 955878f): RESTRICT=network-sandbox
   removed (invalid token) on niri/xwayland-satellite/nwg-look; niri optfeature
   atoms fixed (x11-base/xwayland-satellite, fuzzel line dropped - not in ::gentoo);
   mangowm local USE=asan declared in metadata.xml + S order; matugen S order;
   vesktop domenu; rootapp LICENSE all-rights-reserved; obsidian/scenefx description
   periods; tab-indentation across flagged ebuilds; md5-cache regenerated (egencache).
   Post-fix pkgcheck scan: ZERO WhitespaceFound/DoubleEmptyLine/UnknownUseFlags/
   VariableOrderWrong/DeprecatedInsinto/UnusedInherits/OptfeatureNonexistent/
   UnknownRestrict findings. Remaining findings all accepted-by-design (musl-dev
   NonsolvableDepsInDev for glibc prebuilts, VisibleVcsPkg for live pins,
   Obsidian-EULA custom license, UnknownCategoryDirs informational).
3. Version sweep 100% of 20 packages vs upstream APIs TODAY:
   ALL up-to-date except noctalia-git pin ee8414f10ad5 -> a9cd1c86bdd6 (bumped+pushed
   in 955878f); obsidian stays 1.13.7 (v1.13.8 = APK-only release, re-verified);
   brave-origin 1.94.114 = newest Release-line origin zip (re-verified); helium
   0.15.6.1 newest stable; faugus 2.1.0 = latest release (tags API sorts lexically,
   v1.1-x tags are OLD 2024 tags - false alarm investigated and closed).
4. rootapp teardown re-done: AppImage downloaded, SHA512+BLAKE2B byte-match Manifest,
   X-AppImage-Version=0.9.127 == PV == fedora-nexus spec Version.
5. ROLLING RELEASE DATA FIXES (manual, workflow edits blocked):
   a. Packages index rebuilt with acct-group/{cuse-0,plugdev-0-r3,seat-0-r1}
      entries restored -> index(498)==assets(498) bijection; issue #16 failure mode
      (`no binary packages to satisfy acct-group/seat`) PROVEN FIXED by strict
      emerge --getbinpkg --usepkgonly acct-group/seat rc=0 in clean stage3.
      Persists across Build Relay runs (reconcile keeps published assets).
   b. THREE missing dual-ABI binpkgs built locally + uploaded:
      virtual/libusb-1-r2, virtual/libcrypt-2-r1, sys-libs/libxcrypt-4.5.2
      (all abi_x86_32+abi_x86_64; version races vs official-v3 checked: ours win
      on version or priority). Root cause chain: rolling libsdl3/pipewire/cups/
      libgusb/pam/util-linux/compiler-rt-sanitizers demand dual-ABI virtuals that
      NEITHER nexus NOR official-v3 shipped -> godot strict install failed with
      `no ebuilds built with USE flags to satisfy virtual/libusb:1[abi_x86_32...]`.
      Index now 501 entries == 501 gpkg assets.
6. Docker battle tests (clean stage3:amd64-desktop-openrc, quickstart consumer
   config, live rolling binrepo):
   - acct-group/seat strict install rc=0
   - sys-devel/gcc-16.2.0 strict rc=0 (smoke via gcc-config path)
   - dev-games/godot-4.7.2 FULL quickstart-sequence rc=0: gpm --nodeps bootstrap ->
     BIN_OPTS merge -> godot --version = 4.7.2.stable.gentoo.ed1daf0bf, ldd clean
   - media-video/pipewire-1.6.8 rc=0 ldd clean; x11-libs/gtk+-3.24.52 rc=0;
     media-gfx/blender-bin-5.1.1 rc=0 ldd clean
   - zed consumer simulation: 59 binaries / 0 source builds (respect-use proof)
   - tools/nexus CLI: help/sync(501 pkgs)/list/info all OK in container
7. ncurses<->gpm cycle INVESTIGATED (hard captured-slot-op runtime cycle on fresh
   systems when profile switches enable gpm): quickstart's existing gpm --nodeps
   bootstrap is CORRECT and sufficient; strict --usepkgonly full-matrix runs hit
   the portage cycle error but the documented user flow does NOT. No repo change
   needed; documented here as known edge case.

### Incident record (honesty)
- My FIRST acct-* index upload used versionless CPVs (acct-group/cuse) ->
  Build Relay run 32645966943 (noctalia) crashed parsing it
  (portage InvalidData). Fixed at 15:22 UTC (versioned CPVs); current index
  parse-proven by all consumer tests. Retriggered noctalia as run 32650777962.

### In progress / next steps for THIS run or relay successor
1. AWAIT Build Relay run 32650777962 (gui-wm/noctalia-git from pin a9cd1c86bdd6):
   verify SUCCESS + noctalia-git-9999-N asset replaces old in rolling; then
   install-test the new binary (quickstart flow) and record commits.json match.
2. Final pre-completion gate: full asset list one-version-per-PN proof,
   bash tools/verify-gentoo.sh, adversarial audit, then flip status: complete
   with run_id line. If build still running near timeout: leave status unfinished
   with exactly these continuation notes and trigger relay.
3. OPTIONAL if time: zed full strict install (consumer sim already green),
   blender-bin --version smoke output capture, gcc-config smoke detail.

---
# Previous run records (kept for context)

run_id: 32593909044
status: complete
