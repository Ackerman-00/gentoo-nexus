# gentoo-nexus autonomous-run relay state

status: complete
run_id: 32320581002

## Run summary (run 32320581002, RELAY=false, 2026-08-20 ~01:19Z–~02:00Z)

### BOTH blockers remain; fixes require owner-applied workflow changes

This run verified the repo state and confirmed both blockers from the prior run
(32310336447) are STILL active. Both fixes require editing `.github/workflows/*`
files, which this token cannot push (GitHub App lacks `workflows` scope).

**Blocker 1 — rust-bin `abi_x86_32` use-dep unsatisfiable**
Kills Build Gentoo Official (12+ consecutive failures). Fix: add
`dev-lang/rust abi_x86_32` + `dev-lang/rust-bin abi_x86_32` to GAMING heredoc
in both `build.yml` and `build-gentoo-official.yml`.

**Blocker 2 — gpm/ncurses circular dependency**
Kills Build Relay AND Build Gentoo Official. The gpm/ncurses cycle manifests in
both workflows. Fix: broaden the heal trigger (line ~604 in
build-gentoo-official.yml, equivalent in build.yml) to also fire when installed
ncurses lacks `abi_x86_32`.

**Both fixes are documented in issue #5 (open).**

### Verification work done this run

1. **Ebuild syntax**: ALL 18 ebuilds pass `bash -n`.
2. **Overlay metadata**: `metadata/layout.conf` correct (masters=gentoo,
   thin-manifests, BLAKE2B SHA512). `profiles/repo_name` = gentoo-nexus.
   `profiles/categories` lists all 10 real categories.
3. **Live package EGIT_COMMIT pins**: ALL 3 current (niri, noctalia-v5,
   xwayland-satellite match upstream HEAD exactly).
4. **Version accuracy sweep**: ALL 18 packages verified against upstream:
   - rootapp-bin 0.9.126 — current (AUR bin 0.9.126; upstream URL is rolling)
   - obsidian 1.13.7 — current (GitHub releases)
   - vesktop 1.6.7 — current (GitHub API)
   - protonplus 0.6.4 — current (GitHub API)
   - brave-origin-bin 1.93.137 — current (1 ahead of stable announcement)
   - zen-browser 1.21.15b — current (GitHub API)
   - nwg-look 1.1.1 — current
   - matugen 4.2.0 — current
   - mangowm 0.16.1 — current
   - cliphist 0.7.0 — current (web search falsely reported 0.8.0; no such tag
     exists in sentriz/cliphist or henri-gasc/cliphist)
   - brightnessctl 0.5.1 — current
   - xcur2png 0.7.1-r3 — current (r3 = revision)
   - icoextract 0.3.0 — current
   - scenefx 0.5 — current
   - niri 9999 — EGIT_COMMIT matches upstream HEAD
   - noctalia-v5 9999 — EGIT_COMMIT matches upstream HEAD
   - xwayland-satellite 9999 — EGIT_COMMIT matches upstream HEAD
5. **Rolling release**: 565 assets. 6 overlay packages have stale binaries
   (rootapp-bin, obsidian, protonplus, vesktop, brave-origin-bin, zen-browser)
   — rebuild blocked by both workflow blockers.
6. **Build Relay failures**: also caused by gpm/ncurses cycle (run 32213045126
   confirmed: same `sys-libs/gpm` ↔ `sys-libs/ncurses` circular dep).

### Stale release assets (blocked by workflow fixes)

| Package | Ebuild | Rolling binary | Status |
|---|---|---|---|
| rootapp-bin | 0.9.126 | 0.9.125-1 | STALE |
| obsidian | 1.13.7 | 1.13.4-2 | STALE |
| protonplus | 0.6.4 | 0.5.22-1 | STALE |
| vesktop | 1.6.7 | 1.6.5-2 | STALE |
| brave-origin-bin | 1.93.137 | 1.93.129-3 | STALE |
| zen-browser | 1.21.15b | 1.21.10b-1 | STALE |

### CI status (as of run end)

- Build Gentoo Official: 12+ consecutive failures (Blockers 1+2)
- Build Relay: also failing (Blocker 2 — gpm/ncurses cycle)
- opencode-schedule: 32320581002 (this run) complete

### Issues / PRs

- #5 (open): documents BOTH blockers with docker evidence and exact fixes.
  Owner must apply workflow diffs from a local checkout.
- No open PRs.

### Required owner action

Apply these two diffs to `build.yml` AND `build-gentoo-official.yml` (keep
byte-identical):

**Fix 1** — add to GAMING heredoc (before the `GAMING` terminator):
```
dev-lang/rust abi_x86_32
dev-lang/rust-bin abi_x86_32
```

**Fix 2** — broaden the gpm/ncurses heal trigger:
```bash
# OLD:
if [ -n "$INSTALLED_NCURSES" ] && ! grep -q 'gpm' "/var/db/pkg/${INSTALLED_NCURSES}/USE" 2>/dev/null; then
# NEW:
if [ -n "$INSTALLED_NCURSES" ] && \
   { ! grep -q 'gpm' "/var/db/pkg/${INSTALLED_NCURSES}/USE" 2>/dev/null || \
     ! grep -q 'abi_x86_32' "/var/db/pkg/${INSTALLED_NCURSES}/USE" 2>/dev/null; }; then
```

### What was NOT verified (honest gaps)

- Full docker install-sweep of all 565 release assets: not completed (both
  build pipelines are broken; no new binaries to test).
- No ebuild changes were made this run (all versions current).
- No rebuilds triggered (blocked by workflow issues).
