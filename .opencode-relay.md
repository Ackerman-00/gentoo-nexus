# gentoo-nexus autonomous-run relay state

run_id: 32919847856
status: unfinished

## Run 32919847856 (2026-08-26) — WORK IN PROGRESS

### Done so far (all command-proven, evidence in Actions log)
1. REPO HEALTH AUDIT: all recent CI runs green (Build Relay 32797015537,
   Build Gentoo Official 32847627127/32861605161/32864804529 all success).
2. WORKFLOWS PERMISSION: STILL MISSING (contrary to run instructions).
   `git push` of commit 0e5c0aa (builder-side gpm/ncurses fix) rejected with
   "refusing to allow a GitHub App to create or update workflow ... without
   workflows permission" (reproduced 2026-08-26). Local commit reset; full
   ready-to-apply patch now in issue #19 body (was PLACEHOLDER_DIFF before);
   rejection documented in issue comment. FALLBACK PROTOCOL EXECUTED.
3. POISON RESURRECTION CONFIRMED + PRUNED: Build Gentoo Official rebuilt the
   poisoned pair on 2026-08-25. Torn apart: ncurses-6.5_p20251220-4 metadata
   USE contains `gpm`, RDEPEND=sys-libs/gpm[...]; gpm-1.20.7-r6-21 RDEPEND=
   sys-libs/ncurses:0/6=[...]. DELETED both from rolling + reconciled index.
   NOTE: do NOT trigger ncurses/gpm rebuilds until issue #19 patch lands —
   builder still builds +gpm poison.
4. RELEASE HYGIENE PASS: pruned superseded twins dist-kernel-7.1.9-1,
   gentoo-kernel-7.1.9-1 (::gentoo current = 7.1.10), portage-3.0.82-3
   (::gentoo current = 3.0.82.1; GLSA 202608-12 makes >=3.0.81.2 required).
   Re-added acct-group cuse/plugdev/seat entries lost from Packages when a
   later build regenerated the index (pipeline gap, needs #19-style fix).
   Final state: 502 assets = 499 gpkg + Packages + Packages.gz + commits.json;
   duplicate-PN scan = NONE (libusb pair = virtual/libusb-1-r2 vs
   dev-libs/libusb-1.0.30 = distinct packages); poison scan = empty.
5. VERSION SWEEP 20/20 COMPLETE (upstream APIs, today):
   up-to-date: brightnessctl 0.5.1, cliphist 0.7.0 (=::guru byte-identical
   incl. henri-gasc vendor tarball), nwg-look 1.1.1, rootapp 0.9.127
   (fedora-nexus spec == ours; AppImage URL 200), obsidian 1.13.7 (1.13.8 is
   APK-only upstream), icoextract 0.3.0, faugus-launcher 2.1.0, protonplus
   0.6.4, scenefx 0.5, mangowm 0.16.1, vesktop 1.6.7, helium-browser 0.15.7.1,
   zen-browser 1.21.15b, matugen 4.2.0, xcur2png 0.7.1(-r3), niri pin
   dd75865f == HEAD, xwayland-satellite pin 3bc915f == HEAD.
   UPDATED this run (commit 7ae0ac7): opencode-desktop 1.18.22->1.18.23
   (both debs verified present), brave-origin 1.94.115->1.94.116 (zip URL
   200), noctalia-git pin 8ac887eebb1b->994638f14f2e (upstream HEAD moved).
   Manifests regenerated in clean stage3 (ebuild manifest, fresh downloads),
   md5-cache regen via egencache, stale cache removed. pkgcheck scan on the
   three: only by-design notes (VisibleVcsPkg live pin; musl-dev-profile
   glibc notes). NOTE 2026: pkgcheck now dev-util/pkgcheck in ::gentoo.
6. BUILD TRIGGERED: Build Relay run 32922386621 for
   "dev-util/opencode-desktop www-client/brave-origin gui-wm/noctalia-git"
   force_rebuild=true. Trigger chain verified (run appeared post-push).

### Remaining work (next steps)
1. Poll build 32922386621 to success; verify rolling gets exactly ONE asset
   per touched PN (auto-replace check): opencode-desktop-1.18.23 replaces
   -1.18.22, brave-origin-1.94.116 replaces -1.94.115, noctalia-9999 new rev
   replaces old; commits.json gains 994638f14f2e for noctalia.
2. Install-test the three new binaries in clean stage3-desktop container
   against the LIVE release (+ no-source-rebuild pretend with quickstart
   consumer config) and update coverage ledger.
3. Re-check ncurses/gpm assets after ANY relay run (resurrection trap) —
   prune again if dep-closure resurrects them.
4. Docker battle test: tools/nexus --help/list + quickstart dry-run +
   tests/test_reconcile_path_key.py in clean container.
5. Tear-apart sweep of official-atom gpkg binaries (sample per coverage
   ledger, oldest-first) + install-test batch.
6. Full-binary compatibility pass (install + binary-pulled matrix) within
   time budget; then final gate + status flip.

### Traps / notes for next runner
- Workflows push = BLOCKED for this App token (issue #19 has the patch).
- Builder-side ncurses config still +gpm until #19 lands; never trigger
  sys-libs/ncurses or sys-libs/gpm builds; prune on resurrection.
- acct-group index entries get dropped by every build that re-uploads its
  own Packages; re-add after builds if missing (entries built from gpkg
  metadata; see run script in Actions log of 32919847856).
- cliphist SRC_URI uses henri-gasc fork tarball WITH vendor dir — matches
  ::guru exactly; intentional (vendored Go modules), not a bug.
