# gentoo-nexus autonomous-run relay state

status: complete
run_id: 32215828419

## Run summary (run 32215828419, RELAY=false, 2026-08-19 ~04:27Z–~07:50Z)

### BLOCKER — workflow fixes verified but pushable only by owner (confirmed again)

Single root cause of recurring Build Relay failures, **re-verified this run in
a clean stage3 docker container** with independent evidence:

1. **Heal trigger never fires today.** Stage3 `amd64-desktop-openrc` ships
   installed ncurses `USE="abi_x86_64 ... gpm kernel_linux ..."` (has `gpm`,
   missing `abi_x86_32`) and gpm `USE="abi_x86_64 ... kernel_linux"` (missing
   `abi_x86_32`). The heal block's `grep -q gpm` check passes → heal skipped.

2. **Without heal, resolver deadlocks exactly as reported** — gpm binary ↔
   ncurses ebuild circular dep, `sys-libs/ncurses-6.5_p20251220::__unknown__`
   in "binary packages ignored due to changed dependencies". The nexus rolling
   `ncurses[gpm,abi_x86_32]` binary EXISTS in the release index but is rejected
   by `--binpkg-changed-deps` because the *installed* gpm lacks `abi_x86_32` →
   ncurses source build → cycle. The remote `[nexus]` binrepo alone does NOT
   rescue this (verified: pretend still cycles with remote binrepo configured).

3. **Broadened heal trigger + existing heal body fixes it** (docker-verified):
   `ncurses -gpm` (binary) → `gpm` (binary) → `ncurses +gpm` (source,
   --buildpkg). After that the failing dep chain (`=dev-db/sqlite-3.53.3`)
   resolves with NO circular dep.

**Required owner action (cannot be pushed by this token — `workflows` scope
missing, confirmed by failed push attempt 61a194e):**
- Broaden the gpm/ncurses heal trigger in `build.yml` AND
  `build-gentoo-official.yml` to also fire on missing `abi_x86_32`
  (installed ncurses or gpm). Full diff in issue #5 comment 5336304787.
- Change "Restore GPKG Layout" in both workflows from `<category>/` to
  `<category>/<pn>/` so `emaint binhost --fix` yields a non-empty Packages
  index. Full diff in issue #5 comment 5336304243.
- Both changes were applied locally as commit 61a194e and push was REJECTED
  ("refusing to allow a GitHub App to create or update workflow … without
  workflows permission"); tree reset to 71cca2c. Apply from a local checkout.

**Fresh Build Relay failures this session (all same root cause):**
- 32213045126 FAILED (gpm/ncurses cycle, zen-browser/brave chain)
- 32212549068 FAILED, 32211781030 FAILED (earlier this day)
- Build Gentoo Official 32208530399 FAILED; 32215022082 in_progress

### Stale release assets (builds failed → binaries not refreshed)

9 packages have binaries STALE vs ebuilds (fresh gpkg requires owner's workflow fix):

| Package | Ebuild | Release Asset | Stale? |
|---|---|---|---|
| rootapp-bin | 0.9.126 | 0.9.125-1 | ✗ STALE |
| obsidian | 1.13.7 | 1.13.4-2 | ✗ STALE |
| faugus-launcher | 2.1.0 | 2.0.5-3 | ✗ STALE |
| protonplus | 0.6.4 | 0.5.22-1 | ✗ STALE |
| mangowm | 0.16.1 | 0.15.6-2 | ✗ STALE |
| vesktop | 1.6.7 | 1.6.5-2 | ✗ STALE |
| brave-origin-bin | 1.93.136 | 1.93.129-3 | ✗ STALE |
| zen-browser | 1.21.14b | 1.21.10b-1 | ✗ STALE |
| matugen | 4.2.0 | 4.1.0-2 | ✗ STALE |

All 9 are current upstream (verified via GitHub API `releases/latest`). The
remaining 6 non-live packages + 3 live packages have current binaries.

Live package commits.json vs ebuild pins:
- niri: built 606284464d4a, ebuild e9b215fef4b1 → stale (not rebuilt since pin)
- noctalia-v5: built fa1a968cf730, ebuild b38bf2dde119 → stale (not rebuilt since pin)
- xwayland-satellite: built 3bc915f09dd6, ebuild 3bc915f09dd6 → current ✓

### Version sweep (2026-08-19, independently verified via live GitHub API)

All 18 ebuilds CURRENT; no bumps needed.

| package | ebuild | upstream | status |
|---|---|---|---|
| brightnessctl | 0.5.1 | 0.5.1 | up-to-date |
| cliphist | 0.7.0 | 0.7.0 (sentriz) | up-to-date (note: ebuild uses henri-gasc fork) |
| nwg-look | 1.1.1 | 1.1.1 | up-to-date |
| rootapp-bin | 0.9.126 | rolling AppImage | up-to-date (fedora-nexus matches) |
| obsidian | 1.13.7 | v1.13.7 | up-to-date |
| icoextract | 0.3.0 | 0.3.0 (PyPI) | up-to-date |
| faugus-launcher | 2.1.0 | 2.1.0 | up-to-date |
| protonplus | 0.6.4 | v0.6.4 | up-to-date |
| scenefx | 0.5 | 0.5 tag | up-to-date |
| mangowm | 0.16.1 | 0.16.1 | up-to-date |
| vesktop | 1.6.7 | v1.6.7 | up-to-date |
| brave-origin-bin | 1.93.136 | v1.93.136 (stable, releases/latest) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b | up-to-date |
| matugen | 4.2.0 | v4.2.0 | up-to-date |
| xcur2png | 0.7.1-r3 | 0.7.1 | up-to-date |
| niri | e9b215fef4b1 | e9b215fef4b1 (main HEAD) | up-to-date |
| noctalia-v5 | b38bf2dde119 | b38bf2dde119 (main HEAD) | up-to-date |
| xwayland-satellite | 3bc915f09dd6 | 3bc915f09dd6 (main HEAD) | up-to-date |

### STEP A — Overlay metadata validity (verified this run)

- metadata/layout.conf: masters=gentoo, thin-manifests, BLAKE2B SHA512 ✓
- profiles/repo_name: "gentoo-nexus" ✓
- profiles/categories: 10 categories match actual category dirs ✓
- machine/make.conf: CFLAGS/CPU_FLAGS_X86 match official v3 host ✓
- machine/package.use: VIDEO_CARDS reset, seatd server ✓
- test_reconcile_path_key.py: PASS ✓

### STEP B — Ebuild validity (verified this run)

- bash -n: ALL 18 ebuilds pass ✓
- Required vars (DESCRIPTION, HOMEPAGE, SRC_URI, LICENSE, SLOT, KEYWORDS, RDEPEND): present in all non-live ebuilds ✓
- Live ebuilds (niri, noctalia-v5, xwayland-satellite): EGIT_REPO_URI + EGIT_COMMIT present ✓
- scenefx-0.5: KEYWORDS inside else block (line 14), works correctly for versioned path ✓

### STEP C — SRC_URI reachability (verified this run)

All 15 non-live SRC_URIs return HTTP 200 or 302 (GitHub redirect):
rootapp-bin ✓, brightnessctl ✓, cliphist ✓, nwg-look ✓, obsidian ✓, icoextract (PyPI) ✓, protonplus ✓, faugus-launcher ✓, scenefx ✓, mangowm ✓, vesktop ✓, brave-origin-bin ✓, zen-browser ✓, matugen ✓, xcur2png ✓

### STEP D — Rolling release audit (verified this run)

- 562 total assets in rolling release
- 15/15 non-live ebuild packages have matching .gpkg.tar assets (though 9 are version-stale)
- 3/3 live packages have built .gpkg.tar assets
- commits.json present, consistent with live ebuild EGIT_COMMITs (xwayland-satellite current; niri/noctalia-v5 stale)
- test_reconcile_path_key.py: PASS

### CI status (as of run end)

- Build Relay: 32213045126 FAILED (gpm/ncurses), earlier 32212549068/32211781030 FAILED
- Build Gentoo Official: 32218444427 FAILED, 32216821284 SUCCESS, 32215022082 SUCCESS
- Check Updates: last SUCCESS 32205429421
- opencode-schedule: 32215828419 (this run) in_progress

### Issues / PRs

- #5: root cause re-verified in docker; comment 5337870103 added this run with
  evidence. Owner must apply both diffs from local checkout.
- No open PRs.

### What was NOT verified (honest gaps)

- Full-repo docker sweep + install-verification of all 562 release assets:
  not completed (large; several containers). Overlay install-tests for a
  subset of release binaries remain as future work.
- These gaps are acceptable: no ebuild changes were made this run, so no new
  binaries need install-testing; the 9 stale binaries are blocked by the
  workflow fix.

### Dependency audit table (unchanged from prior run, all deps-verified)

| package | upstream deps found | in ebuild | status |
|---|---|---|---|
| cliphist | wl-clipboard, xdg-utils | ✓ | deps-verified |
| brightnessctl | udev (optional) | ✓ | deps-verified |
| xcur2png | libpng, libXcursor | ✓ | deps-verified |
| matugen | openssl | ✓ | deps-verified |
| rootapp-bin | at-spi2-core, libsecret, glib, icu, nspr, nss, openssl, wayland, alsa-lib, fontconfig, freetype, harfbuzz, libpulse, mesa, vulkan-loader, cups, dbus, glibc, cairo, gdk-pixbuf, gtk+:3, libdrm, libnotify, libX*, libxcb, libxkbcommon, pango, wl-clipboard, xdg-utils, hicolor-icon-theme, xwayland | ✓ | deps-verified |
| nwg-look | at-spi2-core, glib, fontconfig, freetype, harfbuzz, xcur2png, cairo, gdk-pixbuf, gtk+:3, pango | ✓ | deps-verified |
| icoextract | pefile | ✓ (pypi) | deps-verified |
| scenefx | wayland, libdrm, mesa, pixman, libglvnd, libxkbcommon, wlroots | ✓ | deps-verified |
| vesktop | at-spi2-core, libsecret, expat, glib, nspr, nss, wayland, alsa-lib, fontconfig, mesa, cups, dbus, util-linux, glibc, udev, cairo, libdrm, gdk-pixbuf, gtk+:3, libnotify, libX*, libxcb, libxkbcommon, pango, xdg-utils | ✓ | deps-verified |
| zen-browser | at-spi2-core, expat, glib, nspr, nss, alsa-lib, fontconfig, freetype, mesa, cups, dbus, glibc, cairo, gdk-pixbuf, gtk+:3, libX*, libxcb, pango | ✓ | deps-verified |
| brave-origin-bin | at-spi2-core, expat, glib, nspr, nss, alsa-lib, fontconfig, freetype, mesa, cups, dbus, glibc, udev, cairo, gdk-pixbuf, gtk+:3, libnotify, libX*, libxcb, libxkbcommon, pango, xdg-utils | ✓ | deps-verified |
| obsidian | at-spi2-core, libsecret, expat, glib, nspr, nss, wayland, alsa-lib, fontconfig, mesa, cups, dbus, util-linux, glibc, udev, cairo, libdrm, gdk-pixbuf, gtk+:3, libX*, libxcb, libxkbcommon, libxshmfence, pango | ✓ | deps-verified |
| noctalia-v5 | libsecret, nlohmann_json, sdbus-c++, glib, jemalloc, libical, libsodium, libxml2, wayland, librsvg, fontconfig, freetype, harfbuzz, libepoxy, libglvnd, libjxl, libsndfile, libwebp, mesa, pipewire, wireplumber, md4c, tomlplusplus, stb, curl, libqalculate, polkit, pam, cairo, libxkbcommon, pango | ✓ | deps-verified |
| mangowm | wlroots, scenefx, cJSON, glib, libinput, libpcre2, wayland, cairo, libdrm, libxkbcommon, pango, pixman | ✓ | deps-verified |
| xwayland-satellite | wayland, libxcb, xcb-util-cursor, xwayland | ✓ | deps-verified |
| niri | glib, libinput, wayland, libdisplay-info, mesa, seatd, udev, cairo, libxkbcommon, pango, pixman, pipewire (optional) | ✓ | deps-verified |
| protonplus | libadwaita, libarchive, glib, json-glib, libgee, gtk+:4, libsoup:3.0, libsdl3 | ✓ | deps-verified |
| faugus-launcher | pygobject, requests, pillow, vdf, psutil, dbus-python, icoextract, gtk+:4, libadwaita, libmanette, vulkan-loader | ✓ | deps-verified |