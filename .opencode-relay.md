# gentoo-nexus autonomous-run relay state

run_id: 32919847856
status: complete

## Run 32919847856 (2026-08-26) — FINAL REPORT

### Repo-working verdict
YES — proven end-to-end against the LIVE rolling release in clean
gentoo/stage3:amd64-desktop-openrc containers. All 20 ebuilds pass bash -n +
EAPI checks; the Packages index is now FULLY dep-enriched (the day's biggest
find, below); every binary touched or sampled installs via
`emerge --getbinpkg --usepkgonly` with zero missing shared libs
(ldd TOTAL-MISSING=0 across 7 diverse binaries), and the consumer simulation
(`emerge --pretend --verbose --getbinpkg`, machine/ config) shows ZERO source
builds — including the whole gentoo-kernel graph, which compiled 2 packages
from source before today's index fix.

### Root-cause fix of the day: dep-less Packages index silently forced source builds
- SYMPTOM (fresh this run): `emerge --pretend --verbose --getbinpkg
  dev-util/pahole app-text/asciidoc` → both `[ebuild]` + "!!! The following
  binary packages have been ignored due to changed dependencies" in a stock
  consumer container (machine/make.conf EMERGE_DEFAULT_OPTS=--getbinpkg).
- RCA (all command-proven): build-gentoo-official.yml's reconcile writer lost
  DEPEND/RDEPEND/PDEPEND/IDEPEND from META_KEYS (git -S shows the original
  writer had them). Official runs download ALL release assets then re-emit
  every entry through that writer → index-wide dep columns stripped. Current
  portage (--binpkg-changed-deps=auto when not --usepkgonly) ignores remote
  binpkgs whose recorded deps ≠ current evaluation → empty recorded deps =
  always-changed = EVERY nexus binpkg ignored by stock consumers.
  Debug log proof line: `binary: dev-util/pahole-1.30::gentoo` (resolver only
  saw official-v3's older binary, never ours).
- FIX THIS RUN: downloaded all 500 gpkg assets, extracted metadata.tar.zst
  from each, rebuilt the ENTIRE Packages index with CPV + full dep columns
  (DEPEND/RDEPEND/PDEPEND/BDEPEND/IDEPEND), re-uploaded Packages+Packages.gz.
  Re-ran the EXACT failing command → both `[binary]`, zero warnings. Kernel
  consumer pretend went from 2 [ebuild] lines to 0.
- PERMANENT FIX BLOCKED BY PERMISSIONS: workflow hunk #3 appended to issue
  #19 (add the five dep keys back to META_KEYS; BDEPEND also missing from
  build.yml). Until applied, an agent must re-enrich the index after every
  official build.

### Workflows permission status (issue #19)
Push STILL rejected for .github/workflows/* ("refusing to allow a GitHub App
to create or update workflow ... without workflows permission", reproduced
2026-08-26 on commit 0e5c0aa). Fallback protocol executed: local commit reset,
full ready-to-apply patch written into #19 body (was PLACEHOLDER_DIFF), plus
TWO more hunks commented during this run:
  #1 builders pin sys-libs/ncurses -gpm + convergence guard (replaces 3-step heal)
  #2 build-gentoo-official pre-marker filter must consult real asset basenames,
     not just the live index (self-reinforcing entry loss)
  #3 META_KEYS dep columns restoration (above)

### Poison resurrection (ncurses/gpm) — pruned 3x, root cause confirmed live
Build Relay dep-closure rebuilt ncurses[+gpm] during my triggered builds and
re-uploaded it THREE times (-4 at 02:52, -5 at 03:12, -6 at 03:47) because
builder config still carries `sys-libs/ncurses gpm`. Torn apart each time;
metadata USE contains gpm + RDEPEND=sys-libs/gpm[...]. Final state: ncurses
asset DELETED from rolling; consumers fall back to official v3 host's -gpm
ncurses (install tests prove green). DO NOT trigger sys-libs/{ncurses,gpm}
builds until hunk #1 lands. gpm itself was NOT resurrected (not a dep-closure
target); its absence is fine — nothing in the release RDEPENDs on it except
poisoned ncurses, which is gone.

### Rolling release hygiene (REPLACE, NEVER ACCUMULATE)
Post-run state: 502 assets = 499 .gpkg.tar + Packages + Packages.gz +
commits.json. Duplicate-PN scan over final asset list: NONE true dups
(libusb pair = virtual/libusb-1-r2 vs dev-libs/libusb-1.0.30 = distinct).
| package | newest asset kept | stale assets deleted | status |
|---|---|---|---|
| www-client/brave-origin | brave-origin-1.94.116-1 | brave-origin-1.94.115-1 | replaced-clean |
| dev-util/opencode-desktop | opencode-desktop-1.18.23-1 | 1.18.22 asset | replaced-clean |
| gui-wm/noctalia-git | noctalia-git-9999-7 (pin 994638f14f2e) | 9999-6 | replaced-clean |
| sys-kernel/dist-kernel | dist-kernel-7.1.10-1 | dist-kernel-7.1.9-1 | pruned-(1) |
| sys-kernel/gentoo-kernel | gentoo-kernel-7.1.10-1 | gentoo-kernel-7.1.9-1 | pruned-(1) |
| sys-apps/portage | portage-3.0.82.1-3 | portage-3.0.82-3 | pruned-(1) |
| sys-libs/ncurses(+gpm poison) | (none — healthy state until hunk #1) | -4, -5, -6 (all resurrected today) | pruned-(3, poison) |
| acct-group/cuse+plugdev+seat | kept, RE-INDEXED with CPV | 0 | fixed-index |

### Version accuracy sweep (100% of 20-package inventory, upstream APIs today)
| package | packaged version | upstream latest | status |
|---|---|---|---|
| app-misc/brightnessctl | 0.5.1 | 0.5.1 | up-to-date |
| app-misc/cliphist | 0.7.0 | v0.7.0 (=::guru byte-identical incl. vendor tarball) | up-to-date |
| app-misc/nwg-look | 1.1.1 | v1.1.1 | up-to-date |
| app-misc/rootapp | 0.9.127 | 0.9.127 (fedora-nexus spec == ours; AppImage URL 200) | up-to-date |
| app-office/obsidian | 1.13.7 | v1.13.8 APK-only upstream | up-to-date |
| dev-python/icoextract | 0.3.0 | 0.3.0 (tags) | up-to-date |
| dev-util/opencode-desktop | 1.18.22 -> 1.18.23 | v1.18.23 (both debs verified) | UPDATED+BUILT |
| games-util/faugus-launcher | 2.1.0 | 2.1.0 | up-to-date |
| games-util/protonplus | 0.6.4 | v0.6.4 | up-to-date |
| gui-libs/scenefx | 0.5 | 0.5 | up-to-date |
| gui-wm/mangowm | 0.16.1 | 0.16.1 | up-to-date |
| gui-wm/niri | 9999 pin dd75865f547f | HEAD dd75865f547f | up-to-date |
| gui-wm/noctalia-git | 9999 pin 8ac887eebb1b -> 994638f14f2e | HEAD 994638f14f2e | UPDATED+BUILT |
| net-im/vesktop | 1.6.7 | v1.6.7 | up-to-date |
| www-client/brave-origin | 1.94.115 -> 1.94.116 | newest stable-line origin zip (v1.96.x are nightly-only) | UPDATED+BUILT |
| www-client/helium-browser | 0.15.7.1 | 0.15.7.1 (imputnet/helium-linux) | up-to-date |
| www-client/zen-browser | 1.21.15b | 1.21.15b (newest tag) | up-to-date |
| x11-base/xwayland-satellite | 9999 pin 3bc915f09dd6 | HEAD 3bc915f09dd6 | up-to-date |
| x11-misc/matugen | 4.2.0 | v4.2.0 | up-to-date |
| x11-misc/xcur2png | 0.7.1-r3 | 0.7.1 | up-to-date |

### Dependency audit table (100% coverage)
Prior-run teardown retained where upstream unchanged same-week; re-derived for
all bumped packages (fresh downloads inside stage3 via `ebuild manifest`) and
for every package whose shipped binary changed (index rebuild read deps from
each gpkg's own metadata.tar.zst — the strongest possible evidence: the deps
SHIPPED IN THE BINARIES themselves).
| package | upstream deps found | in ebuild | missing/added | status |
|---|---|---|---|---|
| brightnessctl | make/pkg-config | present | none | deps-verified |
| cliphist | wl-clipboard/xdg-utils (=::guru exactly) | present | none | deps-verified |
| nwg-look | gtk+3/glib | present | none | deps-verified |
| rootapp | AppImage NEEDED satisfied (hash-match) | QA_PREBUILT=* | none | deps-verified |
| obsidian | electron deb deps | present | none | deps-verified |
| icoextract | pillow/pefile | present | none | deps-verified |
| opencode-desktop | electron deb deps (deb torn via manifest regen download) | present | none | deps-verified |
| faugus-launcher | python/gtk | present | none | deps-verified |
| protonplus | gtk4/libadwaita | present | none | deps-verified |
| scenefx | wlroots/mesa/wayland | present | none | deps-verified |
| mangowm | wlroots/scenefx stack | present | none | deps-verified |
| niri | smithay stack (=guru) | present | none | deps-verified |
| noctalia-git | meson stack; pin refreshed to HEAD | present | commit bump only | deps-fixed |
| vesktop | electron bundle | present | none | deps-verified |
| brave-origin | chromium zip NEEDED (ldd miss=0 this run) | present | none | deps-verified |
| helium-browser | tar.xz torn prior; unchanged upstream | present | none | deps-verified |
| zen-browser | bundle; unchanged upstream | present | none | deps-verified |
| xwayland-satellite | wlroots C stack | present | none | deps-verified |
| matugen | prebuilt rust binary + openssl | present | none | deps-verified |
| xcur2png | libpng/x11 | present | none | deps-verified |

### Install-test table (clean stage3:amd64-desktop-openrc containers THIS run, LIVE release binrepo)
| package | build | emerge --usepkgonly | smoke test | status |
|---|---|---|---|---|
| sys-kernel/gentoo-kernel-7.1.10 | existing (official run 32847627127-era) | rc=0 | /boot has vmlinuz+initramfs-7.1.10-gentoo-dist.img+System.map+config+grub; amdgpu.ko in modules; equery check failed(0) | installable |
| sys-apps/portage-3.0.82.1 | existing (official run) | rc=0 (batch) | portageq --version = "Portage 3.0.82.1" | installable |
| dev-util/pahole-1.31 | existing (official run) | rc=0 (batch) | pahole --version = v1.31; NOW PULLS AS BINARY (was source-build before index fix) | installable |
| dev-db/sqlite-3.53.4-2 | relay run 32922386621 dep-closure | rc=0 (batch) | sqlite3 --version = 3.53.4 2026-07-24 | installable |
| dev-lang/perl-5.44.0-2 | official run perl-stack self-heal | rc=0 (batch) | perl v5.44.0; ldd miss=0 | installable |
| www-client/brave-origin-1.94.116 | NEW relay 32924457048 | rc=0 | "Brave Origin 152.1.94.116 unknown"; ldd miss=0; equery failed(0) | installable |
| gui-wm/noctalia-git-9999 (994638f14f2e) | NEW relay 32925610684 | rc=0 | "noctalia v5.0.0 (v5.0.0-beta.9-25-g994638f14f2e)" exact pin; commits.json matches; ldd miss=0 | installable |
| dev-util/opencode-desktop-1.18.23 | NEW relay 32922386621 | rc=0 | Electron launches as user w/ --no-sandbox (dbus/X errors = headless-container environmental); equery failed(0) | installable |
| app-text/asciidoc-10.2.1-2 | existing | pretend-only ([binary] proof) | n/a (docs toolchain dep) | installable |

### NO-SOURCE-REBUILD evidence (consumer simulation, machine/ config)
`emerge --pretend --verbose --getbinpkg` shows ZERO [ebuild] lines for:
sys-kernel/gentoo-kernel FULL GRAPH (was 2 source builds before index fix),
brave-origin, noctalia-git, opencode-desktop, pahole, asciidoc, portage,
sqlite. Known non-release exceptions remain as documented in #19: xrdb,
libxmlb (absent from both binhosts; atom-list gap needs owner patch).

### 2026 h. automated excellence checks (fresh evidence, this run)
- pkgcheck scan PASS — in-container scan of the three touched packages
  (dev-util/opencode-desktop, www-client/brave-origin, gui-wm/noctalia-git);
  only by-design notes (VisibleVcsPkg live pin; musl-dev-profile glibc notes).
  NOTE 2026: pkgcheck lives at dev-util/pkgcheck now, NOT app-portage.
- bash -n PASS — 20/20 ebuilds + EAPI var present 20/20 (verifier gate).
- emerge --pretend PASS — consumer simulations above, zero source lines.
- equery check PASS — failed(0) for brave-origin, noctalia-git,
  opencode-desktop after install.
- tc-getPKG_CONFIG / bare pkg-config: no bare invocations in overlay ebuilds.
- Manifest regen via `ebuild manifest` PASS in clean stage3 for
  opencode-desktop-1.18.23 (amd64+arm64 debs freshly downloaded) and
  brave-origin-1.94.116 (zip 184255015B); md5-cache regenerated with
  egencache; stale cache entries removed.

### DOCKER BATTLE TEST evidence
- docker 28.0.4; fresh gentoo/stage3:amd64-desktop-openrc per batch (this
  run: ~8 containers, all fresh docker runs).
- tools/nexus: --help OK; sync fetched live index (499 pkgs); list/search/
  info OK against release. FOUND+FIXED: repo shipped tools/nexus mode 100644
  (non-executable) — commit d1c6d34 sets 755; verified working via python3
  invocation before fix and shebang semantics documented.
- setup/quickstart.sh: help/hardware-menu renders in bare container.
- tests/test_reconcile_path_key.py: PASS.
- ldd sweep: brave/noctalia/portageq/pahole/sqlite3/perl → TOTAL-MISSING=0.

### Official-atom tracking
Check Gentoo Official Updates + Build Gentoo Official all green Aug 25.
::gentoo ground truth verified today: gentoo-kernel 7.1.10 current (shipped),
portage 3.0.82.1 current (shipped; GLSA 202608-12 makes >=3.0.81.2 required),
libusb 1.0.30 current, dist-kernel 7.1.10 current. Multilib spot-check:
abi_x86_32 set unchanged since last audit (wine/steam stack twins present in
release per index USE columns).

### Commits pushed this run (main)
- dea0a42 relay progress checkpoint
- 7ae0ac7 bump: opencode-desktop 1.18.23, brave-origin 1.94.116, noctalia pin 994638f14f2e
- d1c6d34 fix(tools): tools/nexus executable bit (was 100644)
(0e5c0aa workflows fix exists ONLY in issue #19 text — push rejected.)

### Builds triggered & results (trigger chain verified: push -> new run ids)
- 32922386621 Build Relay (opencode-desktop+brave+noctalia, force):
  SUCCESS; shipped opencode-desktop-1.18.23-1; relayed rest.
- 32924457048 Build Relay (relay continuation): SUCCESS; shipped
  brave-origin-1.94.116-1; relayed noctalia.
- 32925610684 Build Relay (relay continuation): SUCCESS; shipped
  noctalia-git-9999-7 at pin 994638f14f2e (commits.json updated).

### Not verified / named gaps
1. Full 499-binary matrix not individually installed this run (time budget);
   covered = all binaries rebuilt-since-last-test (kernels, portage, pahole,
   sqlite, perl stack sample, installkernel via dracut initramfs generation,
   3 bumped packages) + prior-run ledger retained for unchanged binaries.
2. Builder-side fixes (#19 hunks 1-3) NOT applied (workflows permission) —
   ncurses WILL be resurrected by future dep-closures until hunk #1 lands;
   prune procedure: delete asset + re-enrich index (script pattern in
   Actions log of run 32919847856).
3. Index dep-columns enrichment is manual this run; next Build Relay keeps
   dep columns for untouched entries (live-merge) but newly built entries
   lose them until hunk #3 lands — re-check after every official build.
4. Kernel smoke limited to file presence + equery integrity; no actual boot
   test possible in CI container.
5. opencode-desktop GUI smoke is launch-level (no display server available).

### Pre-completion gate checklist
0. Full-binary compatibility pass: SAMPLE-PASS (kernel graph + all rebuilt
   binaries + diverse batch; matrix limitation named above) — PASS-with-named-gap
1. git status --porcelain empty — VERIFIED below (final commit)
2. Every touched package install-tested clean-stage3 — PASS (table above)
3. Coverage ledger updated (this file) — PASS
4. Dependency table 100% inventory — PASS (20/20 rows)
5. Every claim backed by command output in Actions log — PASS
6. Named gaps stated — PASS (above)
7. Relay contains run_id 32919847856 + status complete — THIS COMMIT
8. Release hygiene: exactly ONE .gpkg.tar per PN — PASS (499 unique, dup
   scan = NONE; poison pruned; superseded kernels/portage pruned)

### Next-runner quick-start (if resuming)
1. After ANY official build: check `gh api .../releases/tags/rolling --jq
   '.assets[].name' | grep ncurses` → delete if present; verify Packages
   entries still carry DEPEND/RDEPEND (grep -m1 "^CPV: dev-util/pahole"
   -A30 | grep -c "^RDEPEND:") → re-enrich if stripped (see Actions log).
2. Issue #19 holds all three ready-to-apply workflow patches.
