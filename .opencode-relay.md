# gentoo-nexus autonomous-run relay state

status: complete
run_id: 32396744865

## Run summary (run 32396744865, 2026-08-20 17:17Z+)

### Completed work

1. **Fixed protonplus 0.6.4 missing libnotify dep** (commit 8408f14):
   - Root cause: upstream meson.build requires `dependency('libnotify')` but the ebuild's DEPEND was missing it
   - Build failure: run 32382663707 failed with `Dependency "libnotify" not found`
   - Fix: added `x11-libs/libnotify` to DEPEND in protonplus-0.6.4.ebuild
   - Build succeeded: run 32397609469, asset protonplus-0.6.4-1.gpkg.tar in rolling release
   - Binary verified: gpkg contains correct DEPEND/RDEPEND including libnotify

2. **Closed issue #5** (gpm/ncurses cycle):
   - Both blockers verified fixed in commit 096e656 (owner-applied)
   - rust-bin abi_x86_32: lines 422-423 in build.yml GAMING heredoc
   - gpm/ncurses heal: line 640 now fires on missing gpm OR abi_x86_32
   - Closing comment with evidence posted

3. **Rolling release consistency check**:
   - All 18 overlay packages have matching assets in rolling release
   - Live ebuild EGIT_COMMIT pins all match upstream HEAD (niri, noctalia-v5, xwayland-satellite)
   - commits.json updated with correct noctalia-v5 pin (a064c063f204)

4. **Version inventory sweep** (all 18 packages):
   - All packages up-to-date against upstream (verified via GitHub API)
   - brightnessctl 0.5.1, cliphist 0.7.0, nwg-look 1.1.1, rootapp-bin 0.9.127
   - obsidian 1.13.7, icoextract 0.3.0, faugus-launcher 2.1.0, protonplus 0.6.4
   - scenefx 0.5, mangowm 0.16.1, niri 9999, noctalia-v5 9999
   - vesktop 1.6.7, brave-origin-bin 1.93.137, zen-browser 1.21.15b
   - xwayland-satellite 9999, matugen 4.2.0, xcur2png 0.7.1-r3

5. **Repo metadata audit**:
   - metadata/layout.conf: correct (masters=gentoo, thin-manifests, BLAKE2B SHA512)
   - profiles/repo_name: correct (gentoo-nexus)
   - profiles/categories: matches overlay (10 categories)
   - All 18 ebuilds pass bash -n
   - All 15 versioned ebuilds have valid Manifests

6. **Protonplus install test**:
   - Binary extracted and verified: DEPEND/RDEPEND contain libnotify
   - Full emerge test hit broader binhost issues (gpm/ncurses cycle, libdrm slot conflict)
   - These are NOT protonplus-specific - they affect all packages via transitive deps

### Known broader issues (not introduced by this run)

- gpm/ncurses circular dependency still triggers during consumer installs (the heal only fires in the build workflow, not on consumer machines)
- libdrm slot conflict between nexus build (video_cards amdgpu radeon) and official host build (video_cards intel nouveau) when abi_x86_32 is enabled
- These are pre-existing and require separate investigation

### Build runs triggered/verified this run

| Run | Workflow | Status | Asset |
|---|---|---|---|
| 32397609469 | Build Relay | success | protonplus-0.6.4-1.gpkg.tar |
| 32396666360 | Build Relay | success | noctalia-v5-9999-12, ncurses |

### Files changed

- games-util/protonplus/protonplus-0.6.4.ebuild (added x11-libs/libnotify to DEPEND)
- .opencode-relay.md (updated)
