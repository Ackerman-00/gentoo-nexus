# gentoo-nexus autonomous-run relay state

run_id: 32575880120
status: unfinished

## Run summary (run 32575880120, started 2026-08-22 13:28Z) — IN PROGRESS

### Completed so far

1. **Repo health audit**: layout.conf/repo_name/categories OK; all 18 ebuilds pass
   `bash -n` + EAPI=8; no open issues; no open PRs.

2. **Version sweep (100% of 18)**: ALL up-to-date vs upstream as of 2026-08-22
   (gh api releases/latest + tags). Live pins match upstream HEAD:
   niri dd75865f547f, noctalia-v5 7fd9f7bfa960, xwayland-satellite 3bc915f09dd6.
   obsidian: v1.13.8 is mobile-only .apk → 1.13.7 correct.
   brave-origin-bin 1.94.114 = newest tag publishing stable
   brave-origin-<PV>-linux-amd64.zip (newer tags only carry nightly/beta-infix).
   rootapp-bin: fresh AppImage download X-AppImage-Version=0.9.127; SHA512+BLAKE2B512
   MATCH Manifest; fedora-nexus spec Version 0.9.127.

3. **BUG FOUND + FIXED (commit 444eb08)** — consumer configs rejected builder binpkgs:
   The rolling binhost builds ~139 core atoms WITH abi_x86_32 (build.yml
   package.use/gaming), but machine/package.use + quickstart default did NOT enable
   abi_x86_32 → under default --binpkg-respect-use=y portage IGNORED those binaries:
     `!!! binary packages ignored due to non matching USE: ...abi_x86_32`
   → obsidian/vesktop/zen/brave/mangowm/niri/noctalia/xwl-sat/protonplus/rootapp
   chains pulled nspr/nss/alsa-lib/pipewire/glslang/libdisplay-info/spirv-tools/
   mpg123-base/libfreeaptx/flac/opus as [ebuild] SOURCE builds.
   FIX: machine/package.use + quickstart.sh global_overrides now ship the full
   multilib list by default (minus dev-lang/rust(-bin): not in rolling, kept
   steam-only to avoid forcing a huge local build vs official host).

4. **BUG FOUND — stale shipped binaries (mangowm, noctalia-v5)**: embedded-ebuild
   sweep of ALL 18 rolling assets (streamed metadata.tar.zst vs repo ebuild):
   - gui-wm/mangowm-0.16.1-1.gpkg.tar embeds IUSE="X" but repo has "X asan"
   - gui-wm/noctalia-v5-9999-2.gpkg.tar embeds IUSE="" but repo has "+jemalloc"
   → consumers compile these from source even though binaries exist.
   FIX IN FLIGHT: Build Relay run 32579906503 triggered
   (`force_rebuild=true`, atoms `=gui-wm/mangowm-0.16.1 =gui-wm/noctalia-v5-9999`).
   Other 16/18 assets MATCH their repo ebuilds exactly.

5. **Battle tests passed this run**:
   - tools/nexus --help OK; nexus list OK (live rolling query)
   - setup/quickstart.sh bash -n OK; profile+binrepo steps verified present
   - dep-graph pretend PASS for all 18 atoms (quickstart-equivalent consumer sim)

### In progress / next steps

- [ ] Build Relay 32579906503 (mangowm + noctalia-v5 rebuilds) → poll to success;
      verify mangowm-0.16.1-2 / noctalia-v5-9999-3 assets land in rolling
- [ ] Re-run consumer sim with UPDATED machine/package.use: all 18 atoms must be
      zero-compile ([binary] everywhere incl. mangowm/noctalia own pkgs)
- [ ] Install-test in clean container: mangowm + noctalia-v5 new assets
      (emerge --usepkgonly + equery check + ldd + --version/--help)
- [ ] Monitor Build Gentoo Official 32572952510 (was mid-build at 13:35Z)
- [ ] pkgcheck-scan-equivalent pass over overlay (bash -n done; container scan)
- [ ] Update install-test ledger + dependency audit table below; flip status to complete

### Dependency audit table (all 18, re-derived 2026-08-22)

| Package | upstream deps found | in ebuild | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| brightnessctl | libm/libc/udev | udev | — | — | deps-verified |
| cliphist | go stdlib, wl-clipboard runtime | wl-clipboard, xdg-utils | — | — | deps-verified |
| nwg-look | gtk3, glib, fontconfig, freetype, harfbuzz, cairo, pango, gdk-pixbuf, xcur2png | all present | — | — | deps-verified |
| rootapp-bin | electron bundle deps | all present (QA_PREBUILT) | — | — | deps-verified |
| obsidian | electron deps | all present (QA_PREBUILT) | — | — | deps-verified |
| icoextract | pefile | pefile | — | — | deps-verified |
| faugus-launcher | pygobject, requests, pillow, vdf, psutil, dbus-python, icoextract, gtk4, libadwaita, libmanette, vulkan-loader | all present | — | — | deps-verified |
| protonplus | libadwaita, libarchive, glib, json-glib, libgee, gtk4, libsoup, libsdl3, libnotify | all present | — | — | deps-verified |
| scenefx | wayland, libdrm, mesa, pixman, libglvnd, libxkbcommon, wlroots-0.20 | all present | — | — | deps-verified |
| mangowm | wlroots-0.20, scenefx, cJSON, glib, libinput, pcre2, wayland, cairo, libdrm, libxkbcommon, pango, pixman, linux-headers, xwayland(X) | all present | — | — | deps-verified |
| niri | glib, libinput, wayland, libdisplay-info, mesa, seatd, libudev, cairo, libxkbcommon, pango, pixman, pipewire(screencast) | all present | — | — | deps-verified |
| noctalia-v5 | libsecret, nlohmann_json, sdbus-c++, glib, jemalloc(+), libical, libsodium, libxml2, wayland, librsvg, fontconfig, freetype, harfbuzz, libepoxy, libglvnd, libjxl, libsndfile, libwebp, mesa, pipewire, wireplumber, md4c, tomlplusplus, stb, curl, libqalculate, polkit, pam, cairo, libxkbcommon, pango | all present | — | — | deps-verified |
| vesktop | electron deps | all present (QA_PREBUILT) | — | — | deps-verified |
| brave-origin-bin | electron/chrome deps | all present (QA_PREBUILT) | — | — | deps-verified |
| zen-browser | electron/chrome deps | all present (QA_PREBUILT) | — | — | deps-verified |
| matugen | openssl | openssl | — | — | deps-verified |
| xcur2png | libpng, libXcursor | libpng, libXcursor | — | — | deps-verified |
| xwayland-satellite | wayland, libxcb, xcb-util-cursor, xwayland | all present | — | — | deps-verified |

### Install-test ledger (rolling coverage)

| package | last install-tested | result | smoke result |
|---|---|---|---|
| brightnessctl | 2026-08-22 (prev run 32563813477) | PASS | --version 0.5.1 |
| matugen | 2026-08-22 (prev run 32563813477) | PASS | --version 4.2.0 |
| xcur2png | 2026-08-22 (prev run 32563813477) | PASS | --help OK |
| icoextract | 2026-08-22 (prev run 32563813477) | PASS | --help OK |
| cliphist | 2026-08-22 (this run, pending re-check below) | PENDING | — |
| nwg-look | 2026-08-22 (this run, pending re-check below) | PENDING | — |
| scenefx | 2026-08-22 (this run, pending re-check below) | PENDING | — |
| faugus-launcher | 2026-08-22 (this run, pending re-check below) | PENDING | — |
| protonplus | 2026-08-22 (this run, pending re-check below) | PENDING | — |
| mangowm | awaiting rebuild 32579906503 | PENDING | — |
| noctalia-v5 | awaiting rebuild 32579906503 | PENDING | — |

### Evidence log (commands + outputs this run)

- `bash -n` × 18 ebuilds: PASS
- gh api releases latest × 12 repos + tags for jlu5/icoextract (0.3.0): versions match
- rootapp AppImage sha512 cd43adbc…4948e == Manifest; blake2b512 cb23b4d2…c48d == Manifest
- docker gentoo/stage3:amd64-desktop-openrc sweeps: PRETEND OK ×18 (consumer sim)
- portage warning captured: `binary packages ignored due to non matching USE`
  (=dev-libs/nspr-4.40 abi_x86_32, =gui-wm/mangowm-0.16.1 -asan, …)
- embedded-ebuild sweep: 16 MATCH / 2 STALE (mangowm, noctalia-v5)
- commit 444eb08 pushed (multilib consumer fix); Build Relay 32579906503 dispatched

### What remains

See checklist above. Relay will flip to `status: complete` after build verification.
